export const LESSON_ADAPTATIONS = Object.freeze({
  reducedChoice: Object.freeze({
    id: "reduced-choice",
    label: "Reduced visual choice",
    guidance: "Show two cards at a time and keep the target position variable."
  }),
  motor: Object.freeze({
    id: "motor-access",
    label: "Motor access",
    guidance: "Allow pointing, eye-gaze choice, partner placement, or MIDI/SoundKeys input instead of handwriting."
  }),
  hearing: Object.freeze({
    id: "hearing-access",
    label: "Hearing access",
    guidance: "Keep the spelling visible, face the group, and pair sound information with explicit visual comparison."
  }),
  language: Object.freeze({
    id: "language-support",
    label: "English-language support",
    guidance: "Preteach the meaning of example words with a gesture or object; do not treat unfamiliar vocabulary as a decoding error."
  })
});

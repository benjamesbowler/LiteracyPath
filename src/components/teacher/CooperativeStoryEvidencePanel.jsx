export function CooperativeStoryEvidencePanel({ evidence = [] }) {
  return <section className="cooperative-story-evidence"><h3>Story Crew activity</h3><p>Shared-device role completion only. No names, voices, images, or mastery scores were collected.</p>{evidence.length === 0 ? <p>No cooperative story completed yet.</p> : <ul>{evidence.slice(-5).reverse().map(row => <li key={`${row.questId}:${row.contentVersion}`}><strong>{row.title}</strong><span>{row.focus}</span><small>{row.roles?.join(" → ")} · {row.completedAt ? new Date(row.completedAt).toLocaleDateString() : "Date unavailable"}</small></li>)}</ul>}</section>;
}

// Mission response authority is intentionally closed over by missionReducer.js.
// This boundary remains export-free so no caller can construct brands, consume
// candidate state, finalize a result, or project private commit metadata.

# Sound Seekers audit remediation evidence

**Audit source:** `docs/SOUND_SEEKERS_COMPLETE_AUDIT_2026-07-20.md`
**Evidence snapshot:** 2026-07-21
**Scope:** the current `comic-redesign-audit-fixes` working tree. The source audit is unchanged.

## How to read this ledger

- **Closed (software)** means the audited failure mode has a source-level resolution and a named automated regression check in this tree.
- **Closed locally; external acceptance pending** means the client/runtime is safe, but an honest release claim still depends on evidence that cannot be created in this repository: physical-device or assistive-technology sessions, observed human sessions, deployed database execution, or new human voice recordings.
- A check named in a row is the exact evidence target for that disposition. Merely naming it here is not a substitute for the final clean run; the release owner must use the commands in **Final software gate** on the exact commit being shipped.
- No physical-device, human-observation, production-database, source-pack, or recording evidence has been invented to make the score look complete.

## 2026-07-21 field-regression re-audit

**Trigger:** Field reports said directional controls appeared dead, a physically reached correct letter produced no response, and “Start the adventure again” was undone after exit/re-entry.

**Repository identity (observed):** `git rev-parse --show-toplevel` resolves to `/Users/benjaminbowler/Projects/LiteracyPath` on `comic-redesign-audit-fixes`. A search under `/Users/benjaminbowler/Projects` finds no second LiteracyPath directory, and this repository’s history contains `f79812a9 Complete Sound Seekers audit remediation`. The regressions were therefore not caused by editing the deleted pre-migration checkout. The earlier SS-02 repair fixed stale choice-object rebuilding, but its direct-choice test did not exercise physical route reachability, mobile hit-target overlap, or destructive-reset conflict semantics.

| ID | Disposition | Observed mechanism and implementation | Exact automated evidence |
|---|---|---|---|
| RA-01 · Correct letter physically unreachable | Closed (software); physical-device acceptance remains pending | `src/utils/questSliceSystems.js`; `src/components/quest/world/questPixelRuntime.js` — some resident-relative answer formations lay outside the authored movement wall, so the correct object could be visible yet never enter collision range. An active answer stage now derives the required corridor from the actual rendered answer circles relative to the primary authored route, including moving-answer padding; keyboard/pointer travel widens only while those answers are active. | `tests/unit/questChoiceReachability.test.js` — **“all forty stops expand the active movement corridor to every rendered answer layout”** and the exact Hollow Tree dead-answer fixture. The catalogue scan covers 40 stops, 183 tasks, and 403 physical stages. `tests/browser/quest-input-collision.spec.js` — the real Phaser player physically collides with and advances from the correct `a` at s1, `c` at s5, and `a` at optional-route s16 without invoking a semantic answer button. Focused result: 31/31 movement units and 9/9 live input/collision browser checks. |
| RA-02 · Directional controls appear dead | Closed (software); exact physical-device pairing remains pending | The tested keyboard mapping already handled all four axes. The reproduced software failure was coarse-pointer hit-target occlusion: the pinned semantic answer panel could overlap the D-pad, so covered directions appeared inert depending on viewport/layout. `src/styles/quest.css` now reserves portrait separation, places answers in the opposite landscape safe area, and honors left/right/bottom safe-area insets. | `tests/browser/quest-input-collision.spec.js` — **“all four keyboard arrows move the Beastie on their matching axis”**; **“coarse-pointer answer controls leave every D-pad arrow usable and moving”**; and prompt/answer/D-pad separation at 568×320, 432×320, 424×320, and 360×320. Whole-file result: 9/9. |
| RA-03 · Reset returns old journey after exit/re-entry | Closed locally; Supabase deployment/self-test pending | `src/components/quest/QuestRoot.jsx`; `src/utils/questProgress.js`; `src/utils/progressMerge.js`; `src/utils/questStore.js`; `src/utils/progressQueue.js` — reset previously created a fresh local object, but forward-only cloud/queue merging and a stale tab’s direct write could union the old trail, mastery, purchases, and checkpoint back in. Each reset now carries an independently issued reset ID, observed reset history, and a pending-reset ID set. Stale local, hydrate, and queue generations cannot restore reset journey fields; concurrent pending resets converge without relying on device clocks; server acknowledgement settles pending IDs. Comfort settings and teacher assignment survive while the journey, creature, rewards, mastery, and checkpoint reset. | `tests/unit/questSettingsAndReset.test.js`, `tests/unit/progressMerge.test.js`, and `tests/unit/progressQueue.test.js` cover restart shape, legacy payloads, clock skew, same-ID acknowledgement, unrelated concurrent resets, three-way grouping, stale-tab disk writes, queue coalescing, checkpoint clearing, and assignment/settings retention. Focused result: 52/52. `tests/browser/quest-accessibility.spec.js` — **“start again survives a stale cloud hydrate, close, reopen, and full reload”**, including a simulated suspended stale-tab save: 1/1. `supabase/migrations/20260721100000_sound_seekers_reset_epoch.sql` and the expanded SQL self-test mirror the client rules, but deployed PL/pgSQL behavior is not yet proven. |

## Confirmed findings traceability (SS-01 through SS-71)

| ID | Disposition | Exact implementation | Exact automated evidence / check |
|---|---|---|---|
| SS-01 | Closed (software) | `src/components/quest/QuestRoot.jsx`; `src/utils/lazyWithRetry.js` — retryable lazy world load, local recovery boundary, deterministic fallback to bundled 2D. | `tests/browser/quest-accessibility.spec.js` — **“a failed pixel-world chunk recovers into the bundled 2D trail”**; `npm run test:quest-browser`. |
| SS-02 | Closed (software; strengthened after 2026-07-21 field regression) | `src/components/quest/world/questPixelRuntime.js`; `src/utils/questSliceSystems.js` — every model/stage change rebuilds current answer objects immediately, and each active rendered answer formation expands the legal movement corridor enough for real collision; resize rebuilding remains separately debounced. | `tests/unit/questCorrectAnswerAlwaysResolves.test.js` — **“picking the correct answer always resolves, at every stage of every stop”**; `tests/unit/questChoiceReachability.test.js` — all 40 stops/current 403 generated stages plus the exact Hollow Tree dead-answer fixture; `tests/browser/quest-input-collision.spec.js` — real Phaser collision advances s1, s5, and optional-route s16. |
| SS-03 | Closed (software) | `src/utils/hollowEconomy.js` — exhausted rolled rarity falls through to the other allowed rarity pool before a duplicate is possible. | `tests/unit/hollowEconomy.test.js` — **“silver and gold eggs fall through an exhausted rolled rarity instead of charging for a duplicate”**. |
| SS-04 | Closed (software) | `src/components/quest/world/questPixelRuntime.js` — Phaser `SHUTDOWN` event is wired to idempotent teardown; pending resize timer is cleared and guarded against a dead scene. | `tests/unit/questRuntimeSystems.test.js` — **“pixel-world audit guards cover stage rebuild, teardown, asset failure, motion and hot-path allocation”**. |
| SS-05 | Closed (software) | `src/components/quest/world/questPixelRuntime.js`; `src/components/quest/world/QuestPixelWorld.jsx` — `loaderror` telemetry, neutral child-safe texture fallback, and runtime-signal forwarding. | `tests/unit/questRuntimeSystems.test.js` — **“pixel-world audit guards cover stage rebuild, teardown, asset failure, motion and hot-path allocation”**. |
| SS-06 | Closed (software) | `src/utils/encounterRunCore.js`; `src/components/quest/world/QuestHub.jsx` — first-attempt tally keys one result per encounter/beat, not per physical stage. | `tests/unit/encounterRunCore.test.js` — **“a fumbled then completed multi-stage word contributes exactly one star-tally beat”**; `tests/unit/questRuntimeSystems.test.js` — **“QuestHub audit guards keep scoring beat-based and the encounter render path bounded”**. |
| SS-07 | Closed (software) | `src/utils/questWorldResume.js`; `src/components/quest/world/QuestTrail2D.jsx`; `src/components/quest/world/QuestPixelWorld.jsx` — checkpoint encounter, beat, stage, and phase are clamped against the rebuilt section; 2D also exposes a recovery action if a stage disappears. | `tests/unit/questRuntimeSystems.test.js` — **“world resume checkpoints clamp to rebuilt encounters and never create an actionless trail”**. |
| SS-08 | Closed (software) | `src/content/initialSounds/initialSoundSelector.js` — Initial Sounds mastery now requires two consecutive correct responses. | `tests/unit/initialSoundAuditFixes.test.js` — **“Initial Sounds mastery requires two consecutive correct answers”**. |
| SS-09 | Closed locally; database deployment pending | `src/utils/progressMerge.js`; `supabase/migrations/20260720153000_sound_seekers_audit_integrity.sql` — a fresh device preserves the cloud review-circuit cursor and accepts only a checkpoint matching that cursor. | `tests/unit/progressMerge.test.js` — **“phonics_quest: a synthetic fresh-device cursor does not erase the cloud review position”** and **“phonics_quest: a fresh device resumes only a cloud checkpoint at its merged current stop”**; `supabase/verify/sound_seekers_audit_integrity_selftest.sql` after deployment. |
| SS-10 | Closed (software) | `src/utils/progressQueue.js`; `src/utils/progressSync.js`; `src/components/quest/QuestRoot.jsx` — per-revision v2 queue records, exact snapshot deletion, forward merge, legacy migration, storage-event reconciliation, and reconnect flush. | `tests/unit/progressQueue.test.js` — all six v2 queue tests; `tests/browser/quest-accessibility.spec.js` — **“two open tabs preserve each other's forward quest progress”** and **“simultaneous tabs keep both offline cloud-queue revisions”**. |
| SS-11 | Closed (software) | `src/utils/hollowEconomy.js`; `src/utils/hollowState.js`; `src/components/HollowPage.jsx` — purchase/feed mutations re-read the ledger and re-check affordability at the write boundary. | `tests/unit/hollowEconomy.test.js` — **“the Hollow mutation gate re-checks the fresh ledger before a second fast purchase”**. |
| SS-12 | Closed locally; six physical profiles pending | `src/utils/questDeviceAcceptance.js` — Safari can use sustained, full-window runtime-resource evidence when heap metrics are unavailable; sparse or growing proxies fail. | `tests/unit/questRuntimeSystems.test.js` — **“device acceptance evidence separates local smoke from physical release proof”**; `npm run check:quest-device-harness`; strict proof remains `npm run check:quest-device-acceptance`. |
| SS-13 | Closed (software) | `src/utils/soundSafariRounds.js`; `src/components/learn/games/games/SoundSafariArcadeGame.jsx` — overlapping candidates are resolved in favor of an unambiguous needed sound rather than iteration order. | `tests/unit/soundSafariRounds.test.js` — **“ambiguous overlapping Safari hits cannot turn a correct aim into a false miss”**. |
| SS-14 | Closed (software; duplicate lifecycle finding retained) | `src/components/quest/world/questPixelRuntime.js` — the real Phaser shutdown hook calls idempotent cleanup and clears the stray timeout. | `tests/unit/questRuntimeSystems.test.js` — **“pixel-world audit guards cover stage rebuild, teardown, asset failure, motion and hot-path allocation”**. |
| SS-15 | Closed (software) | `src/components/quest/world/questAssets.js` — imported field avatars clone their geometry before scene-owned disposal. | `tests/unit/questRuntimeSystems.test.js` — **“pixel-world audit guards cover stage rebuild, teardown, asset failure, motion and hot-path allocation”** (asserts `object.geometry = object.geometry.clone()`). |
| SS-16 | Closed (software) | `src/components/quest/world/QuestHub.jsx`; `src/utils/questWorldResume.js` — an empty generated section is immediately gate-ready instead of waiting on an impossible encounter. | `tests/unit/questRuntimeSystems.test.js` — **“QuestHub audit guards keep scoring beat-based and the encounter render path bounded”**; `tests/unit/questRuntimeSystems.test.js` — **“world resume checkpoints clamp to rebuilt encounters and never create an actionless trail”**. |
| SS-17 | Closed (software) | `src/utils/questReviewScheduler.js` — backward/replayed stops cannot produce a negative gap that suppresses box-1 review. | `tests/unit/questReviewScheduler.test.js` — **“backward replay still carries box-1 review instead of producing a negative gap”**. |
| SS-18 | Closed (software) | `src/data/questSequence.js` — cumulative curriculum helpers scan by index predicate rather than assuming source-array order. | `tests/unit/questSequence.test.js` — **“cumulative curriculum helpers are independent of source-array order”**. |
| SS-19 | Closed locally; six physical profiles pending | `src/utils/questDeviceAcceptance.js` — progress must advance strictly; equality/standing still fails smoke and release evidence. | `tests/unit/questRuntimeSystems.test.js` — **“device acceptance evidence separates local smoke from physical release proof”** (explicit zero-progress failures). |
| SS-20 | Closed (software) | `src/utils/questStorageRecovery.js`; `src/utils/questStore.js`; `src/components/quest/QuestRoot.jsx` — save retries after reproducible-cache eviction, bounds only old diagnostics, retains a volatile snapshot, and surfaces an explicit saving warning on total failure. | `tests/unit/questStorageRecovery.test.js` — all four recovery tests; `tests/browser/quest-accessibility.spec.js` — **“a local-storage quota failure warns before the child closes the quest”** and **“a cloud-queue storage failure is surfaced instead of silently dropping the save”**. |
| SS-21 | Closed (software) | `src/components/quest/QuestRoot.jsx` — state mutation and persistence are separated; one scheduled writer owns coalesced saves, with generation-guarded StrictMode cleanup and explicit final flushes. | `tests/browser/quest-accessibility.spec.js` — **“an interrupted journey queues immediately, recovers on reconnect, and resumes its exact task”**; `tests/unit/questRuntimeSystems.test.js` — **“progress sync durably queues before network work and recovers on the online event”**. |
| SS-22 | Closed (software) | `src/components/quest/QuestRoot.jsx`; `src/utils/questTelemetry.js` — offline-shell history is reduced idempotently, so replayed worker history cannot inflate a new session. | `tests/unit/questRuntimeSystems.test.js` — **“offline shell readiness, cold starts, and chapter caches remain honest in the adult report”** (duplicate and replay assertions). |
| SS-23 | Closed locally; database deployment pending | `src/utils/hollowLedgerPolicy.js`; `src/utils/hollowState.js`; `src/utils/progressMerge.js`; `supabase/migrations/20260720153000_sound_seekers_audit_integrity.sql` — deterministic dedupe and bounded purchase/feed histories on both client and server merge paths. | `tests/unit/hollowEconomy.test.js` — **“feeding stops at Grand and append-only Hollow history stays bounded after merge”**; `tests/unit/progressMerge.test.js` — **“server merge mirrors demotion epochs, safe resume, daily missions, and Hollow bounds”**; SQL self-test after deployment. |
| SS-24 | Closed (software) | `src/components/learn/games/games/SoundRacerGame.jsx` — completion is one-shot, and the Done button disables itself before callback re-entry. | `tests/unit/soundSeekersArcadeAudit.test.js` — **“Sound Racer retains its one-shot, cleanup, cache, static-overlay, and reduced-motion guards”**. |
| SS-25 | Closed (software; recording completeness separately pending) | `src/utils/questPhysicalMechanics.js`; `src/components/quest/world/QuestHub.jsx`; `src/components/quest/world/QuestPixelWorld.jsx`; `src/components/quest/QuestRoot.jsx` — missing/muted cues set `recordsMastery:false`; only delivered instructional audio can bank sound mastery. | `tests/unit/questPhysicalMechanics.test.js` — **“muted audio stages become explicit guided play without banking mastery”** and **“no physical sound-to-letter stage asks a child to replay missing audio”**; `tests/unit/questRuntimeSystems.test.js` — QuestHub mastery guard assertions. |
| SS-26 | Closed (software) | `src/utils/soundSafariRounds.js`; `src/components/learn/games/games/SoundSafariArcadeGame.jsx` — stars use units actually presented in the resumed session. | `tests/unit/soundSafariRounds.test.js` — **“a flawless resumed Safari run scores only the units presented this session”**. |
| SS-27 | Closed (software) | `src/content/initialSounds/initialSoundWordBank.js` — distractors exclude alternate graphemes for the same /k/ onset. | `tests/unit/initialSoundAuditFixes.test.js` — **“Initial Sounds never offers two graphemes for the same /k/ onset”**. |
| SS-28 | Closed locally; database deployment pending | `src/utils/questMastery.js`; `src/utils/questReviewScheduler.js`; `src/utils/questReport.js`; `supabase/migrations/20260720153000_sound_seekers_audit_integrity.sql` — independent attempts have their own denominator; timeout and guided exposure remain visible without depressing knowledge accuracy. | `tests/unit/questMastery.test.js` — **“timeout-only exposure is neutral in weakest-target rankings”**, **“a guided tap on the shown answer is compliance, not evidence”**, and **“an assisted MISS still counts as struggle; a timeout counts as nothing but exposure”**; `tests/unit/questRuntimeSystems.test.js` — **“teacher accuracy uses independent attempts while retaining total exposure”**; SQL self-test after deployment. |
| SS-29 | Closed (software) | `src/utils/questReviewScheduler.js`; `src/utils/questMastery.js` — real box promotion (1→2→3→4), immediate miss reset, and interval-aware due logic. | `tests/unit/questReviewScheduler.test.js` — **“promote moves up one box and stops at the top”**, **“a miss sends a sound STRAIGHT BACK to box 1, not one box back”**, and box/due tests; `tests/unit/questMastery.test.js` — **“clean learning recalls advance through Leitner boxes and a miss resets immediately”**. |
| SS-30 | Closed locally; database deployment pending | `src/utils/questMastery.js`; `src/utils/progressMerge.js`; `supabase/migrations/20260720153000_sound_seekers_audit_integrity.sql` — demotion increments an evidence epoch and clears mastery proof so stale shells/sessions/correct counts cannot instantly re-master or resurrect across devices. | `tests/unit/questMastery.test.js` — demotion and anti-cram cases; `tests/unit/progressMerge.test.js` — **“phonics_quest: a newer demotion epoch owns resettable mastery evidence”**; SQL self-test after deployment. |
| SS-31 | Closed (software) | `src/data/questSequence.js` — s17 and s22 no longer advertise impossible `sound-sort` shells. | `tests/unit/questSequence.test.js` — **“single-target and boss stops do not advertise an impossible sound-sort”**; `tests/unit/questPlaythrough.test.js` — **“EVERY DECLARED SHELL HAS A MAPPING AND BUILDS — nothing is silently dropped”**. |
| SS-32 | Closed (software) | `src/utils/soundSafariRounds.js` — every selected Safari ladder task is restricted to a recorded word-audio candidate. | `tests/unit/soundSafariRounds.test.js` — **“every Sound Safari task has recorded gold-voice word audio”**; `npm run check:learn-games`. |
| SS-33 | Closed (software) | `src/components/learn/games/games/SoundSafariArcadeGame.jsx`; `src/utils/learnGamesAudio.js` — digraph reinforcement routes through recorded grapheme/phoneme playback, not word lookup. | `tests/unit/soundSeekersArcadeAudit.test.js` — **“gold voice policy stays recorded-only across shared phonics and login audio”** and **“pre-reader game controls never offer a silent hear-word lifeline”**. |
| SS-34 | Closed (software) | `src/utils/audio/speakWithBrowser.js`; `src/hooks/usePhonicsAudio.js`; `src/components/StudentLoginFlow.jsx`; `src/components/learn/phonics/components/AudioButton.jsx`; `src/components/learn/phonics/components/learning/StepListen.jsx` — browser TTS and oscillator synthesis are disabled; unrecorded speaker actions are gated. | `tests/unit/soundSeekersArcadeAudit.test.js` — **“gold voice policy stays recorded-only across shared phonics and login audio”**; `npm run check:learn-games`. |
| SS-35 | Closed (software) | `src/utils/soundSafariRounds.js` — the hard ladder is filtered to recorded targets and finite unique tasks. | `tests/unit/soundSafariRounds.test.js` — **“Sound Safari uses finite unique words inside each difficulty”** and **“every Sound Safari task has recorded gold-voice word audio”**. |
| SS-36 | Closed (software) | `src/components/learn/games/games/SoundRacerGame.jsx`; `src/components/learn/games/games/SoundBeatGame.jsx` — picture-first onboarding plus existing recorded `listen-and-find`, `tap`, and `great-job` cues; no TTS. | `tests/unit/soundSeekersArcadeAudit.test.js` — **“Sound Racer retains its one-shot, cleanup, cache, static-overlay, and reduced-motion guards”** and **“pre-reader game controls never offer a silent hear-word lifeline”**. |
| SS-37 | Closed (software) | `src/utils/audio/cuePlayer.js` — a failed or rejected clip advances to the next recorded cue instead of aborting the sequence. | `tests/unit/cueAudioLifecycle.test.js` — **“a failed cue advances to the next recorded clip in a sequence”** and **“a rejected cue play promise also advances the sequence”**. |
| SS-38 | Closed (software) | `src/components/quest/RewardScreen.jsx` — recorded `great-job` cue, fanfare, symbol-first reward semantics, timer/audio cleanup, and focus handoff to Continue. | `tests/unit/soundSeekersArcadeAudit.test.js` — **“pre-reader game controls never offer a silent hear-word lifeline”**; `tests/browser/quest-accessibility.spec.js` — reward focus and live reduced-motion tests. |
| SS-39 | Closed (software) | `src/components/quest/world/QuestPixelWorld.jsx` — post-feedback cue timer is stored and cleared on replacement and unmount. | `tests/unit/questRuntimeSystems.test.js` — **“pixel-world audit guards cover stage rebuild, teardown, asset failure, motion and hot-path allocation”**. |
| SS-40 | Closed (software) | `src/components/quest/world/Encounters.jsx` — FlowerPatch/TrailRun recognize recorded word-cue fallback and expose replay only when playable. | `tests/unit/questRuntimeSystems.test.js` — **“pixel-world audit guards cover stage rebuild, teardown, asset failure, motion and hot-path allocation”**; `tests/unit/questPhysicalMechanics.test.js` — **“unrecorded advanced graphemes use audible example-word cues instead of disappearing”**. |
| SS-41 | Closed (software) | `src/utils/questPerformance.js`; `src/components/quest/world/QuestPixelWorld.jsx` — auto mode honors OS reduced-motion and updates live, including older Safari listener APIs. | `tests/unit/questRuntimeSystems.test.js` — **“performance tiers respect explicit accessibility and low-device signals”** and pixel-world motion guards; `tests/browser/quest-accessibility.spec.js` — **“the mounted pixel world follows live OS reduced-motion changes”**. |
| SS-42 | Closed (software) | `src/components/learn/games/games/SoundSafariArcadeGame.jsx` — reduced motion scales the actual simulation delta used by drift, zigzag, flee, and return travel; preference updates live and the listener is removed on teardown. | `tests/unit/soundSeekersArcadeAudit.test.js` — **“Sound Safari reduces actual critter travel and follows live OS motion changes”**. |
| SS-43 | Closed (software) | `src/components/learn/games/games/SoundSafariArcadeGame.jsx` — persistent speaker affordance/replay hit box, repeated word cue after errors, picture-first onboarding, and recorded-only coach support. | `tests/unit/soundSeekersArcadeAudit.test.js` — **“pre-reader game controls never offer a silent hear-word lifeline”**; `tests/unit/soundSafariRounds.test.js`. |
| SS-44 | Closed (software) | `src/components/learn/games/games/SoundRacerGame.jsx` — reduced motion cuts forward speed and parallax, damps ambient movement, and removes ship bob/flicker. | `tests/unit/soundSeekersArcadeAudit.test.js` — **“Sound Racer retains its one-shot, cleanup, cache, static-overlay, and reduced-motion guards”**. |
| SS-45 | Closed (existing behavior verified and guarded) | `src/components/quest/world/QuestHub.jsx` — pickup reward and gate/route transitions pair visual state with distinct star/woosh earcons when sound is enabled; focus moves to the newly available action. | `tests/unit/questRuntimeSystems.test.js` — **“QuestHub audit guards keep scoring beat-based and the encounter render path bounded”**; `tests/browser/quest-accessibility.spec.js` — focus-transition coverage. |
| SS-46 | Closed (software) | `src/components/learn/games/games/SoundRacerGame.jsx` — capture handler consumes only intended activation/steering keys; Tab and browser/AT shortcuts pass through. | `tests/unit/soundSeekersArcadeAudit.test.js` — **“Sound Racer retains its one-shot, cleanup, cache, static-overlay, and reduced-motion guards”**. |
| SS-47 | Closed (software) | `src/components/quest/QuestRoot.jsx` — runtime/telemetry changes schedule and coalesce persistence; view changes, quit, and teardown flush explicitly. | `tests/browser/quest-accessibility.spec.js` — **“an interrupted journey queues immediately, recovers on reconnect, and resumes its exact task”**; `tests/unit/questRuntimeSystems.test.js` — sync durability check. |
| SS-48 | Closed (software) | `src/components/quest/world/questPixelAvatar.js` — creature layout is computed once per sprite-sheet build and reused for all frames. | `tests/unit/questRuntimeSystems.test.js` — **“pixel-world audit guards cover stage rebuild, teardown, asset failure, motion and hot-path allocation”**. |
| SS-49 | Closed (software) | `src/components/quest/world/QuestHub.jsx` — one reusable raycaster, a pre-collected non-recursive occluder list, and throttled blocking refresh replace whole-scene per-choice recasts. | `tests/unit/questRuntimeSystems.test.js` — **“QuestHub audit guards keep scoring beat-based and the encounter render path bounded”**. |
| SS-50 | Closed (software) | `src/components/quest/world/QuestHub.jsx` — active task, encounter tasks, satchel, and the per-frame physical task are memoized/cached by stable encounter-beat keys. | `tests/unit/questRuntimeSystems.test.js` — **“QuestHub audit guards keep scoring beat-based and the encounter render path bounded”**. |
| SS-51 | Closed (software) | `src/components/learn/games/games/SoundRacerGame.jsx` — quality-keyed canvas cache plus idle prewarming prevents every level transition from redrawing the full map synchronously. | `tests/unit/soundSeekersArcadeAudit.test.js` — **“Sound Racer retains its one-shot, cleanup, cache, static-overlay, and reduced-motion guards”**. |
| SS-52 | Closed (software) | `src/components/learn/games/games/SoundRacerGame.jsx`; `src/components/learn/games/shared/threeShell.js` — startup cleanup stack is registered before later constructors can throw; renderer/context, input listeners, resize, loop, idle work, timers, and DOM are all released. | `tests/unit/soundSeekersArcadeAudit.test.js` — **“Sound Racer retains its one-shot, cleanup, cache, static-overlay, and reduced-motion guards”**; `tests/unit/threeShell.test.js`. |
| SS-53 | Closed (software) | `src/components/quest/QuestRoot.jsx` — initial progress is loaded once into state/ref; the old redundant mount load/commit path is removed. | `tests/browser/quest-accessibility.spec.js` persistence/resume coverage; `npm run test:quest-browser`. |
| SS-54 | Closed (software; duplicate configuration finding retained) | `src/components/quest/world/questPixelRuntime.js` — the inert Phaser-4 `resolution` key and false 2×-DPR claim are removed; the deliberately low-cost canvas renderer runs at CSS-pixel resolution. | `tests/unit/questRuntimeSystems.test.js` — **“pixel-world audit guards cover stage rebuild, teardown, asset failure, motion and hot-path allocation”**; `npm run check:quest-pixel`. |
| SS-55 | Closed (software; duplicate configuration finding retained) | `src/components/quest/world/questPixelRuntime.js` — same removal of the unsupported Phaser-4 config; no misleading retina-cap contract remains. | `tests/unit/questRuntimeSystems.test.js` — pixel-world audit guard; `npm run check:quest-pixel`. |
| SS-56 | Closed (software) | `src/components/quest/world/questPixelRuntime.js` — a carried-object signature skips unchanged rebuilds during iOS resize storms. | `tests/unit/questRuntimeSystems.test.js` — **“pixel-world audit guards cover stage rebuild, teardown, asset failure, motion and hot-path allocation”**. |
| SS-57 | Closed (software) | `src/components/quest/world/QuestHub.jsx` — drop and landmark ref callback maps are memoized instead of recreated inline every render. | `tests/unit/questRuntimeSystems.test.js` — **“QuestHub audit guards keep scoring beat-based and the encounter render path bounded”**. |
| SS-58 | Closed (software) | `src/components/quest/QuestRoot.jsx` — answer handling updates state only; normalization/serialization/write happen through the scheduled writer outside the state updater. | `tests/browser/quest-accessibility.spec.js` — interrupted-resume and quota-save coverage; `npm run test:quest-browser`. |
| SS-59 | Closed (software) | `src/components/quest/world/QuestPixelWorld.jsx` — a failed collectible image hides cleanly instead of showing a broken-image glyph. | `tests/unit/questRuntimeSystems.test.js` — **“pixel-world audit guards cover stage rebuild, teardown, asset failure, motion and hot-path allocation”**. |
| SS-60 | Closed (software) | `src/components/quest/world/QuestHub.jsx` — deferred review resets `phonemeFillCount` with the other beat/stage state. | `tests/unit/questRuntimeSystems.test.js` — **“QuestHub audit guards keep scoring beat-based and the encounter render path bounded”**. |
| SS-61 | Closed (software) | `src/components/quest/TrailMap.jsx` — completed journeys suppress both the walker/current position and `is-next` on the finished final stop. | `tests/unit/questRuntimeSystems.test.js` — **“the Den, map and Trading Post keep their complete phone controls”**; `tests/browser/quest-accessibility.spec.js` — mobile chapter map coverage. |
| SS-62 | Closed (software) | `src/components/quest/world/QuestTrail2D.jsx` — the active trail counter is one-based and reaches N/N at the gate. | `tests/unit/questRuntimeSystems.test.js` — **“world resume checkpoints clamp to rebuilt encounters and never create an actionless trail”**; `tests/browser/quest-accessibility.spec.js` accessible-trail coverage. |
| SS-63 | Closed locally; database deployment pending | `src/utils/progressMerge.js`; `supabase/migrations/20260720153000_sound_seekers_audit_integrity.sql` — a fresh device may restore only a cloud checkpoint whose `stopId` matches the merged current cursor; local checkpoints remain authoritative. | `tests/unit/progressMerge.test.js` — **“phonics_quest: checkpoint is resume state — a cloud checkpoint never teleports this device”** and **“phonics_quest: a fresh device resumes only a cloud checkpoint at its merged current stop”**; SQL self-test after deployment. |
| SS-64 | Closed (software) | `src/utils/denRewards.js`; `src/components/HollowPage.jsx` — theme rendering, selection, and loaded active theme all enforce lifetime-gem thresholds. | `tests/unit/hollowEconomy.test.js` — **“Den themes enforce their lifetime-gem unlock thresholds”**. |
| SS-65 | Closed (software) | `src/utils/questProgress.js`; `src/components/quest/QuestRoot.jsx` — replayed gear stops neither re-equip a prior reward nor announce it as new. | `tests/unit/questProgress.test.js` — **“replaying a gear stop neither re-equips nor re-announces old gear”**. |
| SS-66 | Closed (software; human corpus still pending) | `src/components/quest/QuestFieldStudyConsole.jsx`; `src/utils/questHumanAcceptance.js`; `tools/sealQuestHumanObservation.mjs` — duplicate identity includes profile, session, and participant so paired sessions are supported while exact duplicates remain blocked. | `tests/unit/questRuntimeSystems.test.js` — **“human acceptance aggregates cohorts without storing direct identifiers”**; `tests/browser/quest-evidence.spec.js` — import/duplicate coverage. |
| SS-67 | Closed (software) | `src/utils/soundBeatTracks.js`; `src/components/learn/games/games/Ps1ArcadeGame.jsx`; `src/components/learn/games/games/SoundBeatGame.jsx` — attempt-aware mercy widens timing, retains the current beat after the first retry, treats very early taps as guidance, and guarantees eventual non-credit progress. | `tests/unit/soundBeatTracks.test.js` — **“Sound Beat mercy widens timing, retains the current beat, then guarantees progress”**. |
| SS-68 | Closed (software; human corpus still pending) | `src/utils/questHumanAcceptance.js`; `src/components/quest/QuestFieldStudyConsole.jsx`; `docs/validation/quest-human-acceptance/README.md`; `docs/validation/quest-human-acceptance/child-first-use.example.json` — fixed-prefix numeric anonymous codes, direct-identifier rejection, required zero-capable fields, sealed evidence. | `tests/unit/questRuntimeSystems.test.js` — **“human acceptance aggregates cohorts without storing direct identifiers”**; all four `tests/browser/quest-evidence.spec.js` checks; strict corpus gate is `npm run check:quest-human-acceptance`. |
| SS-69 | Closed locally; database deployment pending | `src/utils/progressMerge.js`; `src/utils/progressQueue.js`; `src/utils/questStore.js`; `supabase/migrations/20260720153000_sound_seekers_audit_integrity.sql` — `sanitizeCloudProgressPayload` removes quest telemetry/assignment before queue/upload, and the server merge removes historical cloud telemetry. | `tests/unit/progressMerge.test.js` — **“phonics_quest cloud payloads never include assignment or telemetry”**; `tests/browser/quest-accessibility.spec.js` interrupted/offline test asserts uploaded payload has no telemetry; SQL self-test after deployment. |
| SS-70 | Closed (software) | `src/content/initialSounds/initialSoundSelector.js` — the existing item score is now applied to both letter and item selection using mistake, recency, and prior-correct evidence. | `tests/unit/initialSoundAuditFixes.test.js` — **“item scoring makes repeated mistakes adaptive and avoids a recently answered word”**. |
| SS-71 | Closed (software) | `src/utils/questSliceSystems.js` — progress-style verbs no longer publish a contradictory hardcoded `completed` flag; renderer stage length remains the sole completion contract. | `tests/unit/questSliceSystems.test.js` — **“all later chapters expose real stateful physical verbs”** (turn/steer/signal/climb explicitly assert no magic-threshold `completed` field). |

## Appendix A lead traceability (all 18 leads)

These were unverified leads in the audit. They were re-checked against the current tree; none is silently omitted.

| Lead | Disposition | Exact implementation | Exact automated evidence / check |
|---|---|---|---|
| A-01 · Star Gallery / Grammar Grind shared quality-tier probing | Confirmed and closed (software) | `src/components/learn/games/shared/threeShell.js`; `src/components/learn/games/games/StarGalleryArcadeGame.jsx`; `src/components/learn/games/games/GrammarGrindGame.jsx` — quality is mutable and re-probed on resize/motion change. | `tests/unit/soundSeekersArcadeAudit.test.js` — **“every 3D arcade surface re-probes quality on resize and motion changes”**. |
| A-02 · Two complete Three.js runtimes shipped | Confirmed and closed (software) | All arcade imports resolve through npm `three`; deleted `src/vendor/three/three.min.js`, `src/vendor/three/LICENSE`, and `src/vendor/three/README.md`; Vite no longer bundles the duplicate vendor runtime. | `tests/unit/threeShell.test.js` — **“every 3D arcade surface resolves the shared npm Three.js runtime”**; `npm run check:quest-pixel-bundle`. |
| A-03 · Initial Sounds offers both `c` and `k` | Confirmed; same root as SS-27; closed | `src/content/initialSounds/initialSoundWordBank.js`. | `tests/unit/initialSoundAuditFixes.test.js` — **“Initial Sounds never offers two graphemes for the same /k/ onset”**. |
| A-04 · Frame-budget sampler allocates on ordinary frames | Confirmed and closed (software) | `src/utils/questPerformance.js`; `src/components/quest/world/QuestPixelWorld.jsx`; `src/components/quest/world/QuestHub.jsx` — ordinary/warm-up samples return `null`; a wrapper is created only when a report or quality signal is emitted. | `tests/unit/questRuntimeSystems.test.js` — **“ordinary frame-budget sampling returns no allocation wrapper”**, sustained-failure, and emergency-streak tests. |
| A-05 · Unguarded settings dialog | Confirmed and closed (software) | `src/components/quest/DenScreen.jsx`; `src/styles/quest.css` — guarded native `showModal`/close behavior; when native methods are missing, the fallback sets `open`/`aria-modal`, inerts the background, traps focus, handles Escape, restores trigger focus, and retains complete reset/mobile styling. | `tests/unit/questRuntimeSystems.test.js` — **“Den settings provides a modal fallback without native dialog methods”**; `tests/browser/quest-accessibility.spec.js` — **“settings remains modal when native dialog methods are unavailable”** and **“the mobile release surface keeps the Den and settings child-reachable”**. |
| A-06 · Sound Racer renders WebGL continuously under overlays | Confirmed and closed (software) | `src/components/learn/games/games/SoundRacerGame.jsx` — paused/overlay state renders once until invalidated; texture cache and cleanup are bounded. | `tests/unit/soundSeekersArcadeAudit.test.js` — **“Sound Racer retains its one-shot, cleanup, cache, static-overlay, and reduced-motion guards”**. |
| A-07 · Single-sound signature verbs discard the physical demand | Confirmed and closed (software) | `src/utils/questPhysicalMechanics.js`; `src/utils/questPhysicalPlan.js`; `src/utils/questSliceSystems.js`; `src/components/quest/world/Encounters.jsx` — single-target stops still build authored multi-step physical stages and only credit evidence actually shown. | `tests/unit/questPhysicalMechanics.test.js` — **“every later stop opens with its assigned chapter-authored physical verb”**, **“later signature activities sample a word without crediting unseen pieces”**, and the verb-specific tests; `tests/unit/questSeedwakeVerbs.test.js`. |
| A-08 · “Hear word” lifeline rendered unconditionally | Confirmed and closed (software) | `src/utils/learnGamesAudio.js`; `src/components/learn/games/games/ArcadePracticeGame.jsx`; `src/components/learn/games/games/AdventureGame.jsx`; `src/components/learn/games/games/WordBridgeGame.jsx`; `src/components/learn/games/games/GrammarGrindGame.jsx` — replay controls are available/enabled only for recorded speech. | `tests/unit/soundSeekersArcadeAudit.test.js` — **“pre-reader game controls never offer a silent hear-word lifeline”**. |
| A-09 · 3D arcade quality probed only once | Confirmed and closed (software) | `src/components/learn/games/shared/threeShell.js`; `src/components/learn/games/games/RocketRunGame.jsx`; `src/components/learn/games/games/SoundRacerGame.jsx`; `src/components/learn/games/games/StarGalleryArcadeGame.jsx`; `src/components/learn/games/games/GrammarGrindGame.jsx` — live resize and motion-preference re-probe with cleanup. | `tests/unit/soundSeekersArcadeAudit.test.js` — **“every 3D arcade surface re-probes quality on resize and motion changes”**. |
| A-10 · Correct/incorrect feedback relies on colour | Confirmed and closed (software) | `src/components/quest/world/QuestPixelWorld.jsx`; `src/components/quest/world/QuestTrail2D.jsx`; `src/components/quest/world/QuestHub.jsx`; `src/components/quest/RewardScreen.jsx`; `src/styles/quest.css` — feedback includes text/status, shape/symbol, sound/earcon where available, and live-region semantics rather than colour alone. | `tests/unit/questRuntimeSystems.test.js` — pixel/2D educational-parity guards; `tests/browser/quest-accessibility.spec.js` keyboard/focus and accessible-renderer checks. |
| A-11 · Den / Map hub navigation is text-only | Confirmed and closed (software) | `src/components/quest/DenScreen.jsx`; `src/components/quest/TrailMap.jsx`; `src/components/quest/QuestRoot.jsx` — stable accessible names, non-reading symbols, and transition earcons; headings receive focus. | `tests/unit/questRuntimeSystems.test.js` — **“Den destinations pair non-reading symbols with an audible transition cue”**; `tests/browser/quest-accessibility.spec.js` — **“Den and map transitions move focus to the new screen heading”**. |
| A-12 · No focus management across view changes | Confirmed and closed (software) | `src/components/quest/QuestRoot.jsx`; `src/components/quest/world/QuestHub.jsx`; `src/components/quest/world/QuestPixelWorld.jsx`; `src/components/quest/world/QuestTrail2D.jsx`; `src/components/quest/RewardScreen.jsx`; `src/components/learn/phonics/components/learning/StepTracer.jsx` — new screens/tasks and replaced reward actions receive deterministic focus; tracing exposes semantic stroke buttons that advance one authored stroke and focus Next Step. | `tests/unit/questRuntimeSystems.test.js` — **“QuestRoot hands focus to each newly mounted non-world screen”**; `tests/browser/quest-accessibility.spec.js` — **“the phonics trace step has a keyboard and switch-completable path”**, Den/map focus, and both reward-focus checks. |
| A-13 · Stop s37 Aster Archive alphabet/alternate data | Confirmed documentation drift; closed | `src/data/questSequence.js`; `docs/QUEST_DESIGN_PLAN_2026-07-11.md` — runtime and design plan now agree that s37 explicitly teaches `c_s`, `g_j`, `ch_k`, and `ea_e`, with matching word/sort pools. | `tests/unit/questSequence.test.js` — **“the design plan and runtime agree on the four s37 alternatives”**, plus taught-set, advanced-code, well-formed-teach, and word-pool checks; `npm run check:quest`. Direct recordings for these four alternates remain in the 16-clip external list below. |
| A-14 · TrailRun choice buttons lack accessible labels | Confirmed and closed (software) | `src/components/quest/world/Encounters.jsx` — each fork has an explicit `aria-label` naming its grapheme. | `tests/unit/questRuntimeSystems.test.js` — renderer accessibility/source guards; `tests/browser/quest-accessibility.spec.js` accessible trail checks. |
| A-15 · Proposed voiced instruction/UI copy absent | Load-bearing software path closed; new recordings still pending | `src/components/learn/phonics/components/learning/StepTracer.jsx`; `src/utils/audio/cuePlayer.js`; `src/components/learn/games/games/SoundRacerGame.jsx`; `src/components/learn/games/games/SoundBeatGame.jsx`; `src/components/quest/RewardScreen.jsx` — recorded watch/start/try, listen/find, tap, and praise cues are wired; all fallbacks stay non-synthetic; tracing also has a switch/keyboard-completable semantic path. | `tests/unit/soundSeekersArcadeAudit.test.js` — **“gold voice policy stays recorded-only across shared phonics and login audio”**; `tests/unit/cueAudioLifecycle.test.js`; `tests/browser/quest-accessibility.spec.js` — **“replaying the letter trace demo uses recorded gold-voice instructions”** and **“the phonics trace step has a keyboard and switch-completable path”**. The 16 direct grapheme recordings listed below are still required for full audio acceptance. |
| A-16 · Stale git-tracked hidden duplicate | Confirmed and closed (software) | Deleted the tracked `src/components/quest/world/.fuse_hidden0000000d00000001`; the canonical implementation remains `src/components/quest/world/QuestTrail2D.jsx`. | `git status --short`; `npm run lint`; `npm run build`. |
| A-17 · Initial-Sounds words whose onset is not the target | Confirmed and closed (software) | `src/content/initialSounds/initialSoundWordBank.js`; `src/content/initialSounds/initialSoundMediaEligibility.js`; `src/utils/earlySkills/isRuntimeEligibleEarlySkillQuestion.js` — mismatches remain auditable in the catalogue but are explicitly excluded from scored runtime. | `tests/unit/initialSoundAuditFixes.test.js` — **“phoneme-mismatched and mixed-vowel targets cannot enter scored rounds”**; `npm run check:initial-sound-progression`; `npm run validate:initial-sounds`. |
| A-18 · Vowel banks mix long and short initial vowels | Confirmed and closed (software) | `src/content/initialSounds/initialSoundWordBank.js` — phoneme-mismatch exclusion plus short-vowel-safe level-2 core replacements (`astronaut`, `elevator`, `instructor`, `octagon`, `underground`). | `tests/unit/initialSoundAuditFixes.test.js` — **“phoneme-mismatched and mixed-vowel targets cannot enter scored rounds”**; `npm run check:initial-sound-progression`. |

## Final software gate

Run these on the exact candidate commit; do not substitute a partial test run for the release gate:

```text
npm test
npm run lint
npm run build
npm run check:quest
npm run check:quest-pixel
npm run check:quest-pixel-bundle
npm run check:quest-pacing
npm run check:quest-offline
npm run check:quest-gate
npm run check:quest-route-visual
npm run check:quest-slice-camera
npm run check:learn-games
npm run validate:initial-sounds
npm run check:initial-sound-progression
npm run check:initial-sounds-depth
npm run test:quest-browser
npm run test:quest-offline
npm run test:quest-update
npm run test:quest-soak-smoke
git diff --check
```

`npm run validate:initial-sounds-media` is intentionally not placed in the green-only software block: its repository/runtime pair validation is useful, but its separate source-pack half must remain red until the external Desktop pack exists.

### Recorded verification state at this snapshot

- **PASS:** `npm test` — 680/680 unit tests.
- **PASS:** `npm run build`; `npm run lint` has 0 errors (19 existing repository warnings outside this remediation gate).
- **PASS:** `npm run check:quest-pixel-bundle` — 844.20 KB minified / 227.80 KB gzip / 987 modules, below the unchanged 870 KB budget.
- **PASS:** quest integrity, pixel, pacing, learn-games, music, SFX, gate handoff, three route/topology checks, and all 20 slice-camera viewport/encounter checks.
- **PASS:** Initial Sounds architecture (500 complete pairs), progression (25/25 letters at both levels), and runtime depth (404 selectable image+audio-backed items).
- **PASS:** `npm run test:quest-offline` — 2/2 production offline checks.
- **PASS:** `npm run test:quest-update` — 1/1 installed-update/offline-resume check.
- **PASS (smoke harness only):** `npm run test:quest-soak-smoke` — 1/1 live pixel soak smoke check (36.6 seconds). This is not a substitute for any minimum-20-minute physical-device record.
- **PASS:** `npm run test:quest-browser` — 54/54 complete browser checks, including keyboard/switch tracing, non-native dialog fallback, multi-tab/offline persistence, authored routes, mobile containment, asset budgets, focus, and reduced motion.
- **PASS (focused accessibility spot-check):** 4/4 focused Playwright checks and 53/53 focused unit checks covering recorded StepTracer cues, the semantic keyboard/switch tracing path, and the settings-dialog fallback. The exact regression names are cited in A-05, A-12, and A-15.
- **EXPECTED FAIL, external evidence absent:** strict device acceptance is 0/6; strict human acceptance has 0 records; source-media validation reports 500 complete imported/runtime pairs but no separate Desktop source pack.

## External release blocker checklist

- [ ] **Six physical device / assistive-technology profiles:** current evidence is **0/6** in `docs/validation/quest_device_acceptance.md`. Supply integrity-sealed, minimum-20-minute records for `ipad`, `chromebook`, `android-tablet`, `voiceover`, `nvda`, and `switch`, including real input, offline/reconnect recovery, stable renderer/memory evidence, and operator/device identity. Then run `npm run check:quest-device-acceptance`.
- [ ] **Human observation corpus:** current evidence is **0 valid records** in `docs/validation/quest_human_acceptance.md`. Required aggregate evidence is 8 first-use child sessions, 6 repeat-play child sessions, 8 reward-choice observations, 5 teacher/parent/specialist report interpretations, and 3 distinct classroom-audio environments. Collect only anonymous, consented, integrity-sealed records and run `npm run check:quest-human-acceptance`.
- [ ] **Production-compatible PostgreSQL/Supabase execution:** apply `supabase/migrations/20260720153000_sound_seekers_audit_integrity.sql` and `supabase/migrations/20260721100000_sound_seekers_reset_epoch.sql` in timestamp order to the real target, then execute `supabase/verify/sound_seekers_audit_integrity_selftest.sql`. Static source guards and client tests do not prove deployed PL/pgSQL behavior; this workstation has no `psql`, Supabase CLI, Docker, or Podman runtime available for that execution.
- [ ] **Sixteen direct gold-voice grapheme clips:** record and approve `aw`, `ore`, `air`, `are`, `ear`, `ure`, `le`, `tion`, `y_ie`, `y_ee`, `oo_short`, `ow_ou`, `c_s`, `g_j`, `ch_k`, and `ea_e`. Current runtime behavior is honest—recorded example-word fallback or no mastery credit, never browser TTS—but direct grapheme-audio completeness is not 100% until these recordings exist. `npm run check:quest` reports the gap.
- [ ] **Separate Initial Sounds source-media pack:** restore `/Users/benjaminbowler/Desktop/Kimi_Agent_LiteracyPath a-z assets 500class` or its `initial-sounds-media-pack.zip`, then run `npm run validate:initial-sounds-media`. The repository currently has **500/500 complete runtime image/audio pairs** and **404 runtime-selectable items**, but the external source pack is absent, so provenance/source-pack validation cannot pass honestly.

## Truthful score

- **Confirmed audit findings:** **71/71 (100%) have an explicit software disposition, implementation path, and automated regression target.** No SS identifier is skipped or collapsed into a range.
- **Appendix leads:** **18/18 re-checked and dispositioned.** The software paths are closed or refuted as noted; A-15 still depends on the listed human recordings for full audio acceptance.
- **Release acceptance:** **INCOMPLETE — not 10/10 and not 100% yet.** The code-level remediation ledger is complete, but release acceptance cannot truthfully be awarded while physical evidence is 0/6, human evidence is 0 records, the database self-test has not run on PostgreSQL/Supabase, 16 direct voice clips are absent, and the separate source-media pack is unavailable.

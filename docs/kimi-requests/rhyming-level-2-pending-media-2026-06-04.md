# Kimi Request: Rhyming Level 2 Pending Media

Generated: 2026-06-04

These rime families are intentionally held out of active Rhyming Level 2 coverage until we have at least two clear, child-safe, image-backed rhyme words for each family.

| Rime family | Current app status | Requested media |
| --- | --- | --- |
| ank | `bank` has an image, but there is not a second usable image-backed rhyme word. | Add clear object images and child voice audio for `tank`, `plank`, and `sank`. |
| unk | `skunk` has image/audio, but there is not a second usable image-backed rhyme word in the active resolver. | Add clear object images and child voice audio for `junk`, `trunk`, `bunk`, and `dunk`. |

Image style: match the existing K3 vocabulary media style; one clear object/action per image; no text labels baked into the image.
Audio style: non-AI child voice where possible, clean single-word pronunciation.

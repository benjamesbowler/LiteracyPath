# Release traceability

This ledger is the authoritative status map for `TEN_OUT_OF_TEN_PLAN_2026-07-23.md`.

Status values are limited to `TODO`, `IN-PROGRESS`, `DONE`, `WAIVED`, `EXTERNAL-READY`, and `EXTERNAL-CLOSED`. A row may become `DONE` only when its named gate passes against the reachable product and the evidence column links to the validating commit and release-manifest entry.

| Item | Area | Priority | Status | Named gate | Evidence |
|---|---:|---|---|---|---|
| A1.1 | 1 | P0 | DONE | `check:curriculum-release-standard` | Commit `fd4bda93` introduced the sole versioned owner of per-skill question-count, balance, media, and accessibility rules. Commit `2036416b` extends the final decision with live runtime-selectability: every passing skill publishes only its generated audited question-and-level set; missing IDs, version drift, count drift, or any failing authored dimension returns zero. The strict audit, generated status/exposure, and student loader agree across all 30 skills with zero mismatches. `docs/release/artifacts/2026-07-25T08-41-53-407Z/manifest.partial.json` passes 6/6 against the exact commit: zero-warning lint, all 1,271 unit tests, production build, publication standard, unchanged bundle budgets, and split boundaries. |
| A1.2 | 1 | P0 | DONE | `check:assessment-runtime-variation` + `check:runtime-variation-simulation` | Commit `ebe0ca16`; the reachable selector enforces template, target/item, phoneme, option-set, exact-signature, and content-diversity budgets against the exact question set children receive, while preserving feasible rounds for thin pools. `docs/release/artifacts/2026-07-25T10-24-06-573Z/manifest.partial.json` passes 12/12 against the exact commit: zero-warning lint, 1,276/1,276 unit tests, production build, zero short-audit failures, and 500 deterministic sessions for each of all 30 skills (15,000 sessions total) with zero failures. |
| A1.3 | 1 | P0 | DONE | `check:media-runtime-resolution` | Commit `19b61254`; `docs/release/artifacts/2026-07-24T08-35-48-327Z/manifest.partial.json` passes 4/4 against the committed tree: zero-warning lint, 1,106/1,106 unit tests, production build, and the built-app media gate. The canonical 30-skill board records owner, source/licence, review status, pronunciation variant, inventory, and every explicit release exclusion. The strict audit reduced the documented baseline of 384 wiring defects, 28 required-audio gaps, and 22 required-image gaps to zero; 25 incomplete-media questions are explicitly review-dated and absent from the selectable pool. All 2,398 unique runtime media URLs returned HTTP 200 with nonzero bodies from the production build. |
| A1.4 | 1 | P1 | DONE | strict audit balance report + `check:curriculum-release-standard` | Commit `42a16aaf`; `docs/release/artifacts/2026-07-24T09-13-06-343Z/manifest.partial.json` passes 5/5 against the committed tree: zero-warning lint, 1,112/1,112 unit tests, production build, strict curriculum audit, and canonical publication-standard gate. The versioned standard now caps each Initial Sounds level at 20% per phoneme and 62.5% per prompt family, response format, and listen-and-find interaction. The strict audit publishes exactly 46 items at each level: L1 maximum phoneme 8.7%, interaction 60.9%, listen-and-find 39.1%; L2 maximum phoneme 19.6%, interaction/listen-and-find 60.9%. Both balance rows pass, and the live student loader returns exactly the 92 audited IDs with their audited levels. |
| A1.5 | 1 | P1 | DONE | `check:skill-progression` | Commit `7953d816`; `docs/release/artifacts/2026-07-24T09-24-17-875Z/manifest.partial.json` passes 4/4 against the committed tree: zero-warning lint, 1,115/1,115 unit tests, production build, and the progression gate. The adaptive selector now exhausts unused words globally and prefers older retrieval items over the immediately previous round, eliminating 34 of the original 55 warnings. The remaining 21 mathematically unavoidable events are covered by five narrow, owned, review-dated exceptions; malformed, expired, duplicate, unknown, over-broad, and stale exceptions fail the gate. Final result: 11 managed skills, zero warnings, 21 reviewed exception events, and zero failures. |
| A1.6 | 1 | P1 | DONE | `check:curriculum-board` authenticated admin content-QA route | The generated 30-skill model and authenticated administrator Content Coverage route distinguish authored, approved, exact child exposure, release status/reason, owner, review-dated exclusions, and an exact exposure fingerprint. All 30 skills are READY with strict/status/exposure/loader parity, and both static identity and the live signed-in route pass against the audit backend. |
| A1.7 | 1 | P2 | DONE | Guided Reading student-mode smoke test | Commit `09236bcb`; `docs/release/artifacts/2026-07-24T09-48-40-374Z/manifest.partial.json` passes 4/4 against the committed tree: zero-warning lint, 1,115/1,115 unit tests, production build, and 12/12 reachable-product smoke tests. Student mode presents the child-friendly `Level C` badge and renders none of the internal `level-c`, `moonwood-tales`, or `longer-story-pages` metadata; teacher/admin metadata remains available on its intended surface. |
| A1.8 | 1 | P1 | DONE | `check:assessment-question-integrity` active-image flags | Commits `e42cec1e` and `3e2f583a`; `docs/release/artifacts/2026-07-24T10-03-31-229Z/manifest.partial.json` passes 4/4 against the committed tree: zero-warning lint, 1,116/1,116 unit tests, production build, and the permanent assessment-integrity gate with zero Guided Reading replacement flags. *Shy Comes Out to Play* page 9 is replaced by a visually inspected 1536×864, 276,070-byte series-matched illustration: Shy is correctly the established small grey mouse, all characters are fully visible, and the restored ending is active. The book’s nine-page comprehension provenance hash and ending question were re-reviewed and regenerated. Guided Reading content, visibility, image-quality, and reading-media audits all pass with zero failing live-file issues; the integrity script now exits nonzero on any future unresolved page flag and its permanent `check:release` registration is unit-protected. |
| A1.9 | 1 | P2 | DONE | decoding-support unit + teacher-report route tests | Commit `a9fed892`; `docs/release/artifacts/2026-07-24T11-53-45-757Z/manifest.partial.json` passes 4/4 against the committed tree: zero-warning lint, 1,121/1,121 unit tests, production build, and the permanent Guided Reading decoding-support gate. Repeated word taps now advance through recorded whole-word audio, recorded sound-by-sound support where every grapheme clip exists, and a sentence reread prompt; no browser speech or picture/context guessing cue is introduced. Immutable per-word support events retain book, page, word, stage, true tap time, segments, and audio availability across local/cloud forward merges. The teacher report shows all seeded events and a support-use total. The audit-school fixture now uses canonical book `moonwood-tales-c-25`, carries all three deterministic stages, fails its SQL transaction unless all 25 active learners receive them, and passes the static seed contract. Focused evidence is 60/60 unit checks plus the production report-model/component Playwright route fixture. |
| A1.10 | 1 | P1 EXTERNAL | EXTERNAL-READY | `check:research-pilot-pack` | Commit `d7991b3a`; `docs/release/artifacts/2026-07-24T12-13-34-320Z/manifest.partial.json` passes 4/4 against the committed tree: zero-warning lint, 1,125/1,125 unit tests, production build, and the permanent research-pack readiness gate. `docs/research/` contains 11 complete versioned artifacts: measurement plan for accuracy, latency, 7–21-day retention, unseen transfer, support use and subgroup outcomes; independent all-30-skill expert rubric; child/teacher and Area 2 usability protocols; school/guardian/child/teacher consent, assent, withdrawal and incident templates; strict de-identified data contract; evidence-to-revision workflow; and runnable validation, hashed CSV/JSON export and descriptive-summary scripts. The scripts fail closed on direct identifiers, missing consent/assent, version drift, cross-role sessions, out-of-session timestamps, invalid support order, exposed transfer items, retention outside 7–21 days and subgroup cells below five. Human expert review and pilot execution have not started; no human result, approval, efficacy, fairness or validation claim is made. |
| A2.1 | 2 | P1 | DONE | `check:student-home-hierarchy` policy unit contracts + reachable DOM and screenshot test | Commit `1e68b41b`; `docs/release/artifacts/2026-07-24T12-27-55-196Z/manifest.partial.json` passes 4/4 against the committed tree: zero-warning lint, 1,131/1,131 unit tests, production build, and the permanent student-home hierarchy gate. The versioned canonical policy selects exactly one primary activity from the next available incomplete daily-mission step, falls back deterministically only when needed, partitions every available destination exactly once, and exposes its exact policy/version/source in the DOM. The reachable fresh-learner home renders one dominant `Adventure Map` action sourced from `daily-mission:quest`, exactly two visible secondary choices, and four destinations hidden behind the keyboard-operable `More to explore` disclosure. Desktop and mobile smoke journeys retain working destination actions with no overflow or runtime errors; the visually reviewed 1280×900 baseline permanently guards the intended emphasis. A4.1/A4.2 evidence thresholds remain open and are not claimed by this initial policy boundary. |
| A2.2 | 2 | P1 | DONE | `check:student-home-continuation` policy unit contracts + seeded continuation DOM route test | Commit `fb85caad`; `docs/release/artifacts/2026-07-24T12-35-55-954Z/manifest.partial.json` passes 5/5 against the committed tree: zero-warning lint, 1,134/1,134 unit tests, production build, the unchanged A2.1 hierarchy/screenshot gate, and the permanent continuation gate. The generic `Keep playing` label is gone. Every policy-selected daily activity names itself and the exact number of mission tasks remaining; Sound Seekers reads forward-merged local/cloud progress after hydration and counts unique valid trail IDs against one lightweight 40-trail contract shared with the full quest sequence. The seeded completed-mission learner has 38 valid trails and sees exactly `Continue Sound Seekers — 2 trails left`; activity, goal and remaining count are asserted in the DOM, the action opens Sound Seekers, corrupt/duplicate/out-of-range IDs cannot reduce the goal, and completed progress renders a non-negative replay state. Desktop and mobile smoke tests retain no overflow or runtime errors with the longer specific label. |
| A2.3 | 2 | P1 | DONE | `check:student-daily-mission` pure state contracts + complete-step/celebration/next-route E2E | Commit `49b85d92`; `docs/release/artifacts/2026-07-24T12-47-55-410Z/manifest.partial.json` passes 6/6 against the committed tree: zero-warning lint, 1,138/1,138 unit tests, production build, both prior student-home gates, and the permanent daily-mission gate. The three-step mission now appears in the main content at every viewport with an exact progress element and `done / up next / later` states; the duplicated rail CTA was removed so A2.1 retains one dominant recommended action. Completing a step stores a durable, idempotent per-step celebration, returns to an explicitly named celebration, advances the mission hierarchy and policy recommendation, and makes both the primary card and continuation button launch the exact required mission target (including the selected book/game rather than a generic hub). The E2E completes Adventure Map, sees `Adventure Map complete!` and `Next up: Reading Library`, proves celebration persistence, then routes the primary `Continue Reading Library — 2 tasks left today` action correctly. The critic loop also caught and fixed a pre-existing first-day bug where a fresh learner’s first completed step was saved with an empty date and erased on reload. The refreshed 1280×900 baseline was visually reviewed; desktop/mobile smoke retains no overflow or runtime errors. |
| A2.4 | 2 | P1 | DONE | `check:student-home-card-states` policy contracts + seeded DOM/route/visual matrix | Commit `fb0155a7`; `docs/release/artifacts/2026-07-24T13-05-14-189Z/manifest.partial.json` passes 8/8 against the committed tree: zero-warning lint, 1,141/1,141 unit tests, production build, 12/12 reachable-product smoke tests, all three prior student-home gates, and the permanent card-state gate. Every one of the seven activity cards exposes exactly one `New`, `Continue`, or `Teacher picked` state; progress markers are derived from the real Sound Seekers, Phonics, Adventure Map, Arcade, Story Quest, Guided Reading, and Hollow local/cloud storage contracts. The seeded matrix proves four New, two Continue and one teacher-picked state, rejects raw accuracy, score, percentage, fraction and star-count language, ignores malformed Guided Reading records, and routes the teacher-picked card to Sound Seekers. Both the default and expanded seeded 1280×900 visual baselines were reviewed with no clipping or hierarchy regression. |
| A2.5 | 2 | P2 | DONE | `check:student-rail-accessibility` a11y-name/speech route tests + teacher-owned settings persistence contracts | Commit `3d474a33`; `docs/release/artifacts/2026-07-24T13-30-46-899Z/manifest.partial.json` passes 9/9 against the committed tree: zero-warning lint, 1,146/1,146 unit tests, production build, 12/12 desktop/mobile smoke tests, all four prior student-home gates, and the permanent rail-accessibility gate. Home and all seven destinations now come from one stable ID/label/icon registry used by both home and subpages; every visible place has one exact accessible destination name and a separate 44px `Hear …` control that speaks the same label at a child-paced rate. The browser gate asserts the full eight-place rail, exact speech output, stable icon IDs, and a teacher-reduced four-place rail (Home, Sound Seekers, Phonics, Books) that survives reload; its 1280×900 baseline was visually reviewed. Teachers can toggle the mode per learner from the live roster. Client queue sanitisation and an atomic database trigger preserve teacher ownership across stale/offline child writes while partial teacher writes retain the child's companion, collectibles, and unrelated profile data. |
| A2.6 | 2 | P1 | DONE | `check:student-login-recovery` illustrated recovery-state policy/unit tests + browser E2E | Commit `e6051749`; `docs/release/artifacts/2026-07-24T13-43-58-131Z/manifest.partial.json` passes 10/10 against the committed tree: zero-warning lint, 1,149/1,149 unit tests, production build, 12/12 desktop/mobile smoke tests, all five prior student-side gates, and the permanent login-recovery gate. Code not found, offline, and ask-teacher failures are explicit non-overlapping states with distinct child copy and illustrations; each preserves the typed class code, announces its exact recovery message through an assertive alert, and offers an exact-name 44px replay control backed by an existing recorded voice clip rather than generated speech. Recovery clears on edit, retry, or success; no state depends on colour alone. All three 1280×900 screenshot baselines were visually reviewed. |
| A2.7 | 2 | P3 | DONE | `check:student-login-panel` responsive screenshot + visual-hierarchy/overflow assertions | Commits `808c510b` + `2d5a5f88`; `docs/release/artifacts/2026-07-24T14-00-43-961Z/manifest.partial.json` passes 11/11 against the committed tree: zero-warning lint, 1,149/1,149 unit tests, production build, 12/12 desktop/mobile smoke tests, every A2.1–A2.6 student regression gate, and the permanent login-panel gate. The class-code panel is reduced from 960×860 to a focused 760×560 maximum, the input and primary Go action form one clear column, and freed space holds a concrete six-character code example with an accessible label. The teacher escape remains 44px, underlined, muted, and transparent beneath the primary child path. Login-only shell scoping removes vertical overflow without changing student-home layouts. Desktop 1280×900 and phone 390×844 baselines were visually reviewed; both assert no horizontal or vertical page overflow and preserve the primary/subordinate hierarchy. |
| A2.8 | 2 | P2 | DONE | `check:guided-reading-control-hierarchy` semantic hierarchy/fullscreen browser test + reviewed toolbar baseline | Commit `06ac2a76`; `docs/release/artifacts/2026-07-24T14-11-26-887Z/manifest.partial.json` passes 12/12 against the committed tree: zero-warning lint, 1,149/1,149 unit tests, production build, 12/12 desktop/mobile smoke tests, every A2.1–A2.7 student regression gate, and the permanent Guided Reading hierarchy gate. `Read Page` is the sole primary read-aloud action and carries an explicit priority contract; whole-book audio remains secondary. Page count is a named polite live status, updates under keyboard navigation, and remains visible on phones. Full Screen and Back to Library share a named secondary view-controls group, while fullscreen page navigation remains a separate named group. All child controls retain at least 44px targets outside the compact fullscreen treatment; the reviewed desktop toolbar baseline proves the intended visual hierarchy. |
| A2.9 | 2 | P2 | DONE | `check:sound-racer-tutorial` seeded target/tutorial unit sweep + recorded-audio browser test | Commit `02345c49`; `docs/release/artifacts/2026-07-24T14-22-59-753Z/manifest.partial.json` passes 13/13 against the committed tree: zero-warning lint, 1,150/1,150 unit tests, production build, 12/12 desktop/mobile smoke tests, every A2.1–A2.8 student regression gate, and the permanent Sound Racer tutorial gate. The tutorial is generated from the active track rather than a hard-coded letter: every easy, medium, and hard level across seeds 0, 7, and 42 proves that its displayed target and example onset equal the current level target and that the example has recorded word audio. `Hear the example` plays the recorded target phoneme followed by the recorded example word and does not dismiss the overlay. Phonics and motor guidance are separate named regions. The browser gate opens medium level 3 and proves the live `M → make` example, exact recorded-audio chain, overlay persistence, and distinct steering guidance; its 1280×900 baseline was visually reviewed. |
| A2.10 | 2 | P1 | DONE | `check:fullscreen-overlay-semantics` fail-closed naming contracts + exhaustive real-overlay browser sweep | Commit `c8127a1f`; `docs/release/artifacts/2026-07-24T14-37-42-467Z/manifest.partial.json` passes 14/14 against the committed tree: zero-warning lint, 1,153/1,153 unit tests, production build, 12/12 desktop/mobile smoke tests, every A2.1–A2.9 student regression gate, and the permanent fullscreen-overlay semantics gate. Sound Seekers is now an explicitly named modal whose accessible name follows all six active surfaces: fresh/current creature creator, Den, map, trading post, live trail, and reward. Its close control therefore says exactly `Close Make your creature`, `Close Change your creature`, or `Close Your Den` instead of the generic product name. Every one of the 21 registered games is mounted through the real `GamePlayer` in the browser and proves a title-derived modal name plus exact `Close …` and `Quit …` controls; the checkpoint state also proves an exact title-derived `Resume …` alertdialog. Empty game titles and unknown quest views fail closed in unit contracts. The existing reset/close/reopen/full-reload journey was corrected to use its real preview route and passes with the new surface-derived name. |
| A3.1 | 3 | P2 | DONE | `check:child-surface-rules` registry/unit contract + nine-route real-component browser checklist | Commits `0e0e9bdd` + `4875b358`; `docs/release/artifacts/2026-07-24T16-18-45-969Z/manifest.partial.json` passes 15/15 against exact committed head `4875b358`: zero-warning lint, 1,155/1,155 unit tests, production build, 12/12 desktop/mobile smoke tests, every A2.1–A2.10 student regression gate, and the permanent A3.1 gate. `docs/design/CHILD_SURFACE_RULES.md` defines the required visible title, direct instruction, grouped choices, meaningful progress, and exactly one promoted primary action. The browser mounts all nine real child routes—sign in, home, Phonics, Arcade, Adventure Map, Sound Seekers, Story Quests, Reading Library, and My Hollow—and proves all five regions, exactly one `h1`, exactly one primary marker, and zero page errors. The fixes remove the duplicated home continuation, remove the hero card's second generic action cue, recommend the next unfinished letter/game/map stop/book/story/placement spot, and add missing progress/instructions. The critic loop also found that Sage styling hid the raster Story Quests and Reading Library logos while leaving empty headings; both now have visible text fallbacks. The home hierarchy, full card-state sheet, and reduced-choice rail baselines were rendered and manually reviewed before their intentional updates were committed. |
| A3.2 | 3 | P2 | DONE | `check:key-route-visuals` desktop/phone visual baselines + deterministic delayed-media/route tests | Commit `e86b283f`; `docs/release/artifacts/2026-07-24T16-40-49-263Z/manifest.partial.json` passes 16/16 against exact committed head `e86b283f8b43e2b0e7c1b9ddf9b179e169661df5`: zero-warning lint, 1,155/1,155 unit tests, production build, 12/12 desktop/mobile smoke tests, every retained A2/A3 student regression gate, and the permanent A3.2 gate. Ten manually reviewed 1280×900/390×844 screenshots cover Home, Sound Seekers, Story Quests, Reading Library, and My Hollow with horizontal-overflow assertions. A delayed Home-art test proves a stable labelled placeholder before the real image loads; four delayed lazy-route tests prove named, live loading surfaces with stable geometry before the real Sound Seekers, Story Quests, Reading Library, and Hollow components resolve. The critic loop caught and fixed three defects before acceptance: the Home fixture accidentally reopened first-run companion selection, Reading Library's mobile goal grid expanded to 824px and clipped, and the Home mobile rail inherited sheet spacing that made the header overlap the page. |
| A3.3 | 3 | P0 | DONE | `check:a11y-routes` | Commit `a8fd481dcd0d0a0cc778deac23a50adbda4ce4dd`; `docs/release/artifacts/2026-07-24T17-23-47-649Z/manifest.partial.json` passes 17/17 against the committed tree: zero-warning lint, 1,157/1,157 unit tests, production build, 12/12 desktop/mobile smoke tests, every prior student regression gate, and 48/48 serious/critical Axe route checks. The canonical inventory covers nine primary student routes, eight authenticated-teacher-equivalent seeded routes, seven real key modal states, and fixed 1280×900 plus 390×844 viewports. The teacher harness renders the production sidebar, Today, Classes, Assess, Progress, Resources, report, EL benchmark, Guided Reading, question guide, learner drawer, class-code, and discontinue surfaces with deterministic owned class/learner/evidence data rather than bypassing components or relying on an external password. The critic loop found and fixed low-contrast Home, Phonics, and Story Quest states; invalid Guided Reading tablist children; mobile teacher modals blocked by the learner drawer; and an unnamed nested Sound Racer dialog. Route-set uniqueness, both roles, both viewports, all seven modal contracts, zero page errors, and zero serious/critical Axe violations are permanently gated in `check:release`. Three intentional accessible-colour snapshot changes were visually reviewed without changing tolerances. |
| A3.4 | 3 | P1 | DONE | `check:learner-accessibility-settings` | Commit `fb903498f0fdf143488e6151c2a19c67acf0d45d`; `docs/release/artifacts/2026-07-24T18-00-21-911Z/manifest.partial.json` passes 18/18 against the committed tree: zero-warning lint, 1,162/1,162 unit tests, production build, 12/12 desktop/mobile smoke tests, all prior student, visual, child-surface, fullscreen, and all-route accessibility gates, plus the new focused five-setting policy and reachable-product E2E. Teachers can persist exactly five fail-closed settings per learner: reduced effects, untimed/extended response, lower audio intensity, narration, and simplified backgrounds. The student runtime hydrates them from the learner profile; extended response removes Trail Run's countdown and timeout, lower intensity scales real recorded, synthetic, music, and ambience volume to 55%, narration automatically reads supported recorded Guided Reading pages without falsely counting a manual support action, reduced effects suppresses decorative transitions and celebrations, and simplified backgrounds removes non-instructional scenery while retaining teaching imagery. Client sanitisation and an atomic database merge preserve teacher/admin ownership across stale or offline child writes while retaining unrelated profile data. The deterministic browser gate toggles all five in the production teacher dialog, asserts the exact persisted payload, reloads the profile, and proves every learner-side effect including recorded narration volume. The critic loop found and fixed the phone dialog's initially hidden actions and a later legacy 40px button override; the final 390×844 primary action is fully visible and exactly 44px, and desktop/phone dialog plus learner narration imagery were visually reviewed. |
| A3.5 | 3 | P1 | DONE | `check:overlay-contrast-focus` | Commits `50011588` + `2f1b653159878cdaf54c2ca954451fe062475d36`; `docs/release/artifacts/2026-07-24T18-33-30-289Z/manifest.partial.json` passes 19/19 against the exact committed tree: zero-warning lint, 1,162/1,162 unit tests, production build, 12/12 desktop/mobile smoke tests, every retained student/visual/fullscreen/all-route accessibility gate, and the new overlay sweep. All 21 registered game overlays and their real quit prompts receive automated Axe colour-contrast checks plus keyboard focus verification for every visible enabled control; all 21 are repeated under OS forced-colours mode with a 4px system-highlight focus indicator. Sound Racer's dark real tutorial separately proves readable low-opacity text and visible focus for both instructional actions. Standard and in-product high-contrast Sound Seekers creator states prove that locked creature choices stay at full opacity with readable labels, prices, and non-colour borders while enabled choices retain a 4px focus ring. The gate's first run found a real 2.7:1 CVC Word Builder difficulty badge, which was replaced by a shared AA-safe treatment. The critic loop also found that the older overlay-semantic journey raced lazy game onboarding and that the phone visual baseline still expected faded locked choices; the journey now waits for the real lazy component and dismisses its active onboarding before testing the parent quit prompt, and the reviewed 390×844 baseline clearly preserves all six creature choices and their lock prices. Standard, high-contrast, forced-colours, Sound Racer, desktop creator, and phone creator renders were visually reviewed. |
| A3.6 | 3 | P1 | DONE | `check:device-matrix` | Commit `97e4778c`; `docs/release/artifacts/2026-07-24T19-07-43-364Z/manifest.partial.json` passes 20/20 against the committed tree: zero-warning lint, 1,165/1,165 unit tests, production build, 12/12 smoke tests, all retained student UX/accessibility gates, and the permanent device-matrix gate. The matrix reviews 68 screenshot baselines: every one of nine student routes at six small-phone/tablet/Chromebook/projector profiles, two software-keyboard viewports, and game/Guided Reading/Story Quest fullscreen transitions at four display profiles. Every route asserts zero horizontal overflow, a visible keyboard-focus outline, and at least 44×44px for every visible enabled control; compact Guided Reading additionally proves overflowed words remain scroll-reachable. The pass fixed undersized map markers, flex-shrinking game controls, undersized fullscreen reader controls/word help, and a Story Quest progress-callback render loop, with fullscreen runtime errors now gate-failing. |
| A3.7 | 3 | P2 | DONE | `check:guided-reading-measure` | Commits `335b56ea`, `58a7e4de`, and `5cb66943`; `docs/release/artifacts/2026-07-24T20-08-10-856Z/manifest.partial.json` passes 21/21 against the exact committed tree: zero-warning lint, 1,169/1,169 unit tests, production build, 12/12 smoke tests, every retained student UX/accessibility gate, the permanent reading-measure gate, and the six-profile device matrix. Guided Reading levels A/B/C now own explicit line-length, type-size/leading, image/text-ratio, and stacked-image templates; the browser gate measures rendered characters on all 23 pages of three canonical reference books, proves the intended desktop ratios, and guards 44px reader controls. Optional Line Focus highlights one sentence without dimming surrounding text and follows a tapped word. Three level baselines plus phone-landscape, tablet, Chromebook, projector, and toolbar regressions were visually reviewed. The critic loop fixed squeezed fullscreen controls and an unreadably small landscape illustration; it also exposed false-shallow overlay evidence, leading to semantic first-run controls and complete lazy-game focus/contrast coverage rather than accepting a flaky release result. |
| A3.8 | 3 | P2 | DONE | `check:locked-item-affordance` (DOM, spoken names, icons, disabled contrast) + retained visual/a11y/device gates | `docs/release/artifacts/2026-07-24T20-51-53-821Z/manifest.partial.json` (22/22, exact `cb01e5bee19b61a3ce7bb64b4214a96632b4b209`) |
| A3.9 | 3 | P2 | DONE | `check:student-emphasis-budget` (all 9 child routes × desktop/phone) + retained key-route/a11y/device gates | `docs/release/artifacts/2026-07-24T21-34-14-084Z/manifest.partial.json` (23/23, exact `491486df76c2eeac332c8c356f1f43be17f2b9ce`) |
| A3.10 | 3 | P1 | DONE | `check:assessment-media-evidence` failed-item/shared-source exclusion, fixed-length refill, unscored live 404 replacement, evidence-name and decoration audit + retained visual/a11y/device gates | Commit `d26ec9c4df5420f6744a1080ea1a8fed9152da4f`; `docs/release/artifacts/2026-07-24T22-01-38-044Z/manifest.partial.json` passes 24/24 against the exact committed tree: zero-warning lint, 1,180/1,180 unit tests, production build, 12/12 desktop/mobile smoke journeys, every retained student UX/accessibility gate, and the permanent A3.10 gate. Every live assessment stimulus, prompt image, and visual answer now uses one fail-closed evidence component with a meaningful equivalent accessible name and explicit evidence role; generic names are replaced from the represented item, while unnamed evidence throws rather than silently shipping. The listening glyph is explicitly decorative and hidden. A real browser 404 proves the failed item and another item sharing its source are removed, the two-item round is refilled from safe candidates, progress remains at question 1, no answer/feedback/mastery evidence is recorded, replacement assets load, and Axe reports zero serious/critical findings. The critic loop also found and corrected an invalid labelled progress-dot container by making it a fully valued progressbar. |
| A4.1 | 4 | P1 | DONE | `check:learning-policy` minimum-evidence, recency, confidence, progression, and sparse-learner route contracts | Commits `61486dd1` and `6486acd3`; `docs/release/artifacts/2026-07-24T23-04-07-698Z/manifest.partial.json` passes 7/7 against exact commit `6486acd36166428d80c379234af420812010435e`: zero-warning lint, 1,185/1,185 unit tests, production build, 12/12 smoke journeys, student-home policy regression, the permanent learning-policy gate, and export compatibility. The policy requires 8 scored learner responses or 3 independent exact-item attempts, evidence inside 90 days, explicit confidence, and the 85%/70% Secure/Developing boundaries before a conclusion or progression is allowed. Every conclusion carries policy version `2026.07.24-a4`. The sparse Amara route renders “Not enough evidence,” never “100% accuracy” or a reteach instruction; its sparse exact item is also withheld, and Axe reports zero serious/critical findings. |
| A4.2 | 4 | P1 | DONE | `check:learning-policy` canonical threshold/vocabulary grep guard across conclusion surfaces | Commits `61486dd1` and `6486acd3`; `docs/release/artifacts/2026-07-24T23-04-07-698Z/manifest.partial.json` passes 7/7 against exact commit `6486acd36166428d80c379234af420812010435e`. One documented module now owns accuracy bands, minimum evidence, recency, confidence, progression, class comparison, and canonical Secure/Developing/Needs support/Not enough evidence/Not checked wording. The permanent guard scans 15 live teacher, dashboard, report, historical-summary, and export conclusion surfaces and rejects presentation-layer numeric thresholds or legacy status copy. Semantic export goldens were reviewed cell-by-cell: 218 intentional vocabulary/evidence-policy changes, with identical sheet, row, and cell shapes; all 3 snapshots, lazy CSP-safe loading, and the 500-row memory check pass. Student-home recommendation versioning remains independently stable rather than inheriting the A4 evidence-policy version. |
| A4.3 | 4 | P1 | DONE | `check:class-summary-accuracy` learner/response aggregation, comparability suppression, DOM, visual, and Axe contracts | Commit `49f1b9b94b840e73b46f94ea2a738c54fe4135bf`; `docs/release/artifacts/2026-07-24T23-22-29-919Z/manifest.partial.json` passes 7/7 against that exact tree: zero-warning lint, 1,188/1,188 unit tests, production build, 12/12 smoke journeys, retained learning-policy coverage, the permanent A4.3 gate, and export compatibility. Reachable teacher class summaries now show learner-weighted accuracy with the policy-ready learner denominator and response-weighted accuracy with the scored-response denominator; raw correct counts are used when present, while legacy rows have a tested accuracy × response fallback instead of becoming false zeroes. Policy version `2026.07.24-a4.3` permits a single learner-weighted headline only with at least 2 policy-ready learners, 70% policy-ready coverage, and no worse than 4:1 response imbalance; otherwise both descriptive views remain visible and the single headline is explicitly suppressed with reasons. The browser fixture proves 66% learner-weighted versus 71.9% response-weighted from 2/3 ready learners and 32 responses, shows “No single class average,” and has zero serious/critical Axe findings. Desktop and 390px visual review passed. Critic loops fixed invalid definition-list semantics, a `Number(null) → 0` legacy fallback defect, and the canonical Not-enough-evidence chart segment’s stale colour selector. |
| A4.4 | 4 | P1 | DONE | `check:recommendation-explanations` versioned contract, complete surface registry, child/teacher DOM, and Axe tests | Commit `d5ee7230de040e3b5313e4f49288770dbe9d055a`; `docs/release/artifacts/2026-07-24T23-41-33-875Z/manifest.partial.json` passes 8/8 against that exact committed tree: zero-warning lint, 1,191/1,191 unit tests, production build, 12/12 smoke journeys, student-home hierarchy, learning-policy and class-summary regressions, and the permanent recommendation-explanation gate. One versioned policy now registers all 5 live child recommendation surfaces (home, phonics, Adventure Map, Arcade, Guided Reading) and all 5 live teacher surfaces (dashboard next steps, Today, Progress, Guided Reading, targeted review); the source-inventory guard fails if registry or implementation drifts. Child explanations are visible, plain-language, and capped at 120 characters. Teacher disclosures cannot render without explicit Evidence, Dependency, Confidence, and Unlock fields, and are attached to reteach, inactivity, celebration, assessment-due, group, outlier, book, and targeted-review recommendations. Reachable DOM tests prove both audience contracts and policy/surface/version provenance with zero serious/critical Axe findings. Desktop visual review passed; the critic loop replaced an initially example-only gate with the complete audience-specific registry, corrected card semantics/layout for nested teacher detail, and clarified that celebration comparisons remain private rather than labelling learners publicly. |
| A4.5 | 4 | P1 | DONE | `lint --max-warnings=0` + focused regression tests | `docs/release/artifacts/2026-07-23T12-46-12-124Z/manifest.partial.json`: lint, 976 unit tests, build, 10 desktop/mobile smoke journeys, authenticated teacher/admin routes, and bundle budget all pass |
| A4.6 | 4 | P0 | DONE | `check:release-readiness-surface` + export completeness | `docs/release/artifacts/2026-07-23T13-06-59-764Z/manifest.partial.json`: reachable signed-in route exports all 520 seeded attempts and 520 unique question records; checkpoint persistence no longer caps remaining/mastered/covered item lists |
| A4.7 | 4 | P1 | DONE | `check:engagement-sync` durable queue/retry, class-health, concurrent-write, offline-reload, DOM, and Axe contracts | Commit `914cd9581e4c97d601fe8a86f958087637171ca7`; `docs/release/artifacts/2026-07-25T00-13-57-787Z/manifest.partial.json` passes 7/7 against that exact tree: zero-warning lint, 1,201/1,201 unit tests, production build, 12/12 smoke journeys, retained learning-policy coverage, the permanent engagement-sync gate, and database bootstrap ordering. Child learning events are persisted before delivery, use idempotent event IDs, retry with bounded exponential backoff, survive an offline reload, and remain non-blocking when storage is unavailable. A real-browser chaos test cuts the network mid-write and proves one recovered delivery with zero loss; another overlaps two forced flushes and proves one RPC and one delivery count. A versioned class panel on teacher and admin surfaces separates waiting/recovered/potentially-lost events, raises an alert above the published 1% loss threshold, exposes storage failures, and reports unavailable telemetry without changing learner evidence; desktop and 390px visual review plus Axe serious/critical checks pass. The critic loop fixed reloads bypassing backoff, storage-full counter loss and retry storms, concurrent double-counting, untrusted client observation times, missing volatile pending age, stale-session rescheduling, and a production style-generator collision. |
| A4.8 | 4 | P0 | DONE | `check:curriculum-composed` one-command correctness ∧ depth ∧ variation ∧ media ∧ runtime-selectability proof | Commit `25dac6da110e899b2c75afcc93bb89451681a87c` makes the synthetic composed result independently runnable, expands it to every required dependency, and retains the identical composition in the credentialed full release. Exact manifest `docs/release/artifacts/2026-07-25T11-58-03-296Z/manifest.partial.json` passes 10/10 against that commit: all five global dimensions pass, all 30/30 audited skills are release-ready per skill, all 15,000 simulated sessions pass, 2,410/2,410 built media URLs resolve, and active media scans report zero wiring, missing-media, known-unsuitable, excluded-target, or semantic-conflict failures. |
| A4.9 | 4 | P0 | DONE | route-level teacher-data + product-finish gates | `docs/release/artifacts/2026-07-23T13-53-43-693Z/manifest.partial.json`: authenticated teacher Reports renders the formal EL scope picker, PDF and Excel actions, uncapped saved history, explicit retention policy, redownload, and confirmed cloud/browser deletion |
| A4.10 | 4 | P1 EXTERNAL | EXTERNAL-READY | `check:calibration-readiness` item/threshold model, seeded admin dashboard, no-fake-results guard, and specialist protocol | Commit `4e5e4b46ac9f8d445c1956f4e9893fe6a9bb988b` adds the admin-only Calibration section with deterministic synthetic difficulty, possible reteach over-identification, declared subgroup, and ability-band-matched differential-item monitoring. Its 72 participants and 1,152 events are repeatedly labeled seeded preview—not child evidence—and cannot alter learning policy or item release. Cells below five are suppressed; contradictory follow-up and matched-gap rows create specialist review candidates rather than false-positive, causal, unfairness, or bias conclusions. `docs/research/CALIBRATION_PROTOCOL.md` freezes administration, versions, sampling, item difficulty, threshold stability, adjudication, subgroup, DIF, missingness, decision, signature, and re-review requirements. Exact manifest `docs/release/artifacts/2026-07-25T11-38-10-170Z/manifest.partial.json` passes 7/7 against that commit: zero-warning lint, all 1,288 unit tests, production build, the research pack, desktop/mobile calibration browser journeys, and unchanged bundle/split guards. Human execution remains `not_started`; all human-result and certification fields remain false. |
| A5.1 | 5 | P1 | DONE | teacher-nav DOM + old-route redirect tests | Commits `b73649dd` and `20b73e6b`; `docs/release/artifacts/2026-07-23T16-25-01-884Z/manifest.partial.json` passes 8/8 against the committed tree: lint is clean, 983 unit tests pass, production build and bundle budgets pass, the reachable teacher nav exposes exactly Today · Classes · Assess · Progress · Plan/Resources with contextual modules, stored module routes redirect to their intention, and the complete signed-in report/export journeys remain reachable. The route race found by the first evidence run was fixed at its stale async redirect and the isolated 520-item path then passed three consecutive runs. |
| A5.2 | 5 | P0 | DONE | seeded Today briefing E2E | Commit `b1459888`; `docs/release/artifacts/2026-07-23T16-37-54-606Z/manifest.partial.json` passes 9/9 against the committed tree: zero-warning lint, 986 unit tests, production build, bundle ratchet, the complete teacher/report/export routes, and a dedicated signed-in seeded Today gate. The reachable briefing separates class administration from four required zones (attention, due, changed, direct actions), with low-attainment flags withheld below eight responses, visible evidence and policy bases, real current/prior seven-day counts, selected-class continuity, and working learner review, assessment, progress, and class-management actions. |
| A5.3 | 5 | P1 | DONE | persistent teacher-context E2E | Commit `058e4bc5`; `docs/release/artifacts/2026-07-23T16-57-02-154Z/manifest.partial.json` passes 10/10 against the committed tree: zero-warning lint, 986 unit tests, production build, bundle ratchet, Today/IA/class/report/export regressions, and the dedicated context gate. The teacher route now owns class, instructional-group, and learner context independently from the child session; the seeded E2E drills through an evidence group and three learners, proves the student-session ID remains empty, preserves class/group/learner in URL and stored state, and restores the exact learner drawer after reload. The gate also caught and closed a boot race where asynchronous view-transition hydration could be overwritten by the default Today redirect. |
| A5.4 | 5 | P0 | DONE | dashboard parity matrix + route test + dead-branch grep guard | `docs/release/artifacts/2026-07-23T13-27-33-171Z/manifest.partial.json`: 32 parity rows resolved, canonical teacher marker rendered, admin dashboard absent, and no dashboard mode switch remains |
| A5.5 | 5 | P1 | DONE | fresh-teacher onboarding E2E | Commits `a3f1efcc` and `aa4f49bb`; `docs/release/artifacts/2026-07-23T17-19-00-402Z/manifest.partial.json` passes 14/14 against the committed tree: zero-warning lint, 986 unit tests, production build, bundle and database-schema gates, live four-teacher Auth/RLS isolation, all prior teacher/report/export regressions, and two dedicated onboarding journeys. The manual path starts at 0/4 and reaches 4/4 only after a real class, atomic roster import, teacher-set login pictures, and a saved assessment response. The sample path creates one transactionally safe, unmistakably labelled fictional class with three login-ready learners, verifies every learner has zero practice evidence, and remains at 3/4 until a genuine check is run. Seed reapplication restores both audit accounts to a provably fresh state. |
| A5.6 | 5 | P1 | DONE | roster-scale import/bulk/archive/transfer E2E | Commit `ec48f526`; `docs/release/artifacts/2026-07-23T17-39-42-124Z/manifest.partial.json` passes 15/15 against the committed tree: zero-warning lint, 986 unit tests, production build, bundle and database-schema gates, live Auth/RLS isolation, every prior teacher/report/export regression, and the dedicated real-class roster gate. The E2E imports a quoted 28-row CSV into a seeded class, identifies and explains two case-insensitive duplicates before saving, creates 26 learners atomically, proves the resulting 39-learner instructional group and search/sort/filter controls, previews two complete school/class/login cards, archives one learner without deleting evidence, and transfers another learner with the same identity and history to a different class. Active and archived rosters are now separate, archived learners can be restored, transfers cannot target the current class, printing uses an accessible DOM preview, and deterministic seed reapplication removes all mutable test residue. |
| A5.7 | 5 | P2 | DONE | class-code feedback interaction + a11y test | Commit `2ae80a4c`; `docs/release/artifacts/2026-07-23T17-47-27-183Z/manifest.partial.json` passes 16/16 against the committed tree: zero-warning lint, 986 unit tests, production build, bundle and database-schema gates, live Auth/RLS isolation, all prior teacher/report/export regressions, and the dedicated class-code interaction gate. Copying reports the exact copied code through a polite live region and gives a manual fallback when clipboard access is unavailable. Regeneration now uses an accessible in-product modal that states the old code stops immediately, shared devices need the replacement, and learner accounts, login pictures, progress, and assessment evidence remain intact; only the deliberately labelled action calls the server, the new code is rendered and announced, failures preserve the current code, and the E2E proves no native browser dialog occurs. |
| A5.8 | 5 | P1 | DONE | print-route test + `document.write` grep guard | Commit `ce24fc17`; `docs/release/artifacts/2026-07-23T17-58-04-390Z/manifest.partial.json` passes 17/17 against the committed tree: zero-warning lint, 989 unit tests, production build, bundle and database-schema gates, live Auth/RLS isolation, all prior teacher/report/export regressions, and the dedicated print-route/source-safety gate. Login cards now open on an accessible, page-sized route with exact school, class, class-code, learner, and symbol context; four cards are laid out per A4 sheet, the screen preview exposes page counts and a live readiness announcement, print output removes application chrome, and returning preserves the exact roster context. The CSP blocker was removed at its root across login cards, worksheets, practice packs, presentations, and certificates by replacing document-stream mutation with a tested Blob-document loader; the repository gate recursively scans every JS/JSX/TS/TSX source file and fails if either dotted or bracketed document writes return. |
| A5.9 | 5 | P2 | DONE | teacher student-preview safety E2E | Commit `d7d8319d`; `docs/release/artifacts/2026-07-23T18-07-31-759Z/manifest.partial.json` passes 18/18 against the committed tree: zero-warning lint, 989 unit tests, production build, bundle and database-schema gates, live Auth/RLS isolation, all prior teacher/report/export regressions, and the dedicated preview-safety gate. Student preview now carries a persistent “Previewing as…” banner, records the exact class/group/learner and originating teacher screen, isolates browser storage under a teacher-preview scope, and blocks cloud progress writes before they enter the save queue. The E2E opens a real Story Quest, attempts a real progress write, verifies the learner’s stored progress is byte-for-byte unchanged, observes the accessible blocked-write announcement, and returns in one click to the identical teacher URL and context. |
| A5.10 | 5 | P1 | DONE | intervention lifecycle E2E + live ownership/state verifier | Commit `96cd769c`; `docs/release/artifacts/2026-07-23T18-24-25-103Z/manifest.partial.json` passes 19/19 against the committed tree: zero-warning lint, 992 unit tests, production build, unchanged bundle budgets, reconstructable database schema, deterministic audit seed, live Auth/RLS isolation, every prior teacher/report/export regression, and the dedicated intervention gate. Evidence suggestions now open a required owner/date/group/focus/activity plan; delivery, observed outcome, review date, and follow-up are durable lifecycle states. The database enforces ordered transitions, class membership for every learner, teacher ownership, and atomic parent-to-follow-up linkage. Ineffective and overdue work resurfaces on Today; the E2E completes plan → deliver → record → review, reloads to prove persistence, sees the follow-up, creates it, and proves the original alert clears. The live verifier separately rejects cross-teacher reads/writes, a foreign-class learner, and a planned-to-recorded state skip. |
| A6.1 | 6 | P0 | IN-PROGRESS | deterministic audit-school seed used in CI | SQL assertions and live Auth/RLS gate pass locally; CI manifest evidence pending P0.5 |
| A6.2 | 6 | P1 | DONE | `check:teacher-roster-device-matrix` + platform-neutral visual baselines | Commit `36974fd9`; `docs/release/artifacts/2026-07-23T18-55-52-428Z/manifest.partial.json` passes 21/21 against the committed tree: zero-warning lint, 992 unit tests, production build, unchanged bundle budgets, reconstructable database schema, deterministic audit seed, live Auth/RLS isolation, every prior teacher/report/export regression, authenticated accessibility, and the dedicated device matrix. The roster now defaults to the five high-signal teacher columns, exposes Sound Seekers and Login as optional saved columns, and retains the choice across reloads. At Chromebook 1366×768 the default table fits without horizontal scrolling; at tablet 1024×768 it becomes a two-column semantic card grid with contained controls and 44px learner actions. The real right-side learner drawer adds login and Sound Seekers detail without making those columns permanent, takes initial focus, closes with Escape, restores focus, remains non-blocking for teacher navigation, and stays inside both viewports. Four committed screenshots cover the roster and drawer at both sizes, with platform-neutral paths and a 2.5% rendering tolerance. |
| A6.3 | 6 | P2 | DONE | `check:teacher-contextual-help` + authenticated Axe + visual baseline | Commits `d9fd00f5` and `bc084ef6`; `docs/release/artifacts/2026-07-23T19-11-19-541Z/manifest.partial.json` passes 22/22 against the committed tree: zero-warning lint, 992 unit tests, production build, unchanged bundle budgets, reconstructable database schema, deterministic audit seed, live Auth/RLS isolation, every prior teacher/report/export/device regression, the contextual-help gate, and authenticated accessibility. The 18-row question-type reference is no longer mounted inline in the Classes dashboard. Teachers open a searchable plain-language guide directly from the roster in one action or from learner evidence in two; search spans the check name, child task, skill proved, and teaching response to a miss. The dialog focuses search, reports result counts, supports Escape/focus return and keyboard scrolling, has zero serious/critical Axe violations, and has a platform-neutral visual baseline. The first release attempt correctly failed because the new help action changed the tablet roster screenshot by 4%; the reviewed baseline was updated without changing the 2.5% tolerance, and the complete run then passed. |
| A6.4 | 6 | P1 | DONE | `check:teacher-urgency-order` + authenticated teacher regression + Chromebook/tablet visual matrix + authenticated Axe | Commit `250df2d5`; `docs/release/artifacts/2026-07-23T19-31-01-811Z/manifest.partial.json` passes 23/23 against the committed tree: zero-warning lint, 992 unit tests, production build, unchanged bundle budgets, reconstructable database schema, deterministic audit seed, live Auth/RLS isolation, every prior teacher/report/export/device regression, the new urgency-order gate, and authenticated accessibility. The DOM now orders setup blockers before Today actions, intervention follow-up, and the class pulse. On Classes, the class pulse precedes roster administration, which is collapsed by default for ready classes and remains keyboard-operable. Explicit class creation, sample-class creation, and setup continuation open the roster work the teacher just initiated, so the saved onboarding path remains uninterrupted. Contextual help stays directly reachable outside the collapsed block. The device gate caught a 30px width regression from nesting the roster; full roster width was restored before evidence was accepted. The intentional 52px tablet-height reduction from relocating contextual help was visually reviewed and baselined without changing the 2.5% tolerance. |
| A6.5 | 6 | P1 | DONE | `check:teacher-action-feedback` adoption guard + authenticated announcement/undo/print/export test + authenticated Axe | Commits `d01bcc32` and `23f806f9`; `docs/release/artifacts/2026-07-23T19-53-23-439Z/manifest.partial.json` passes 24/24 against commit `23f806f98754bef23cc15ac2aba33760a26c0099`: zero-warning lint, 995 unit tests, production build, unchanged bundle budgets, reconstructable database schema, deterministic audit seed, live Auth/RLS isolation, every prior teacher/report/export/device regression, the shared-feedback gate, and authenticated accessibility. One semantic `ActionFeedback` pattern now covers pending, success, error, and undo states for clipboard, sync, loading, login-card/report printing, report exports/downloads/deletes, and roster operations. Errors use assertive alerts; other updates use atomic polite status regions; pending work exposes busy state and respects reduced motion. Archive confirmation offers a working restore action. A source guard rejects the replaced ad-hoc patterns. The bundle ratchet blocked an eager main-entry adoption, so the EL-hub instance was isolated in its own lazy chunk without increasing any budget. The first evidence run also blocked on deterministic one-pixel Chromebook/tablet roster height shifts; both images were visually and pixel-metric reviewed, refreshed without changing the 2.5% tolerance, and the complete suite then passed. |
| A6.6 | 6 | P1 | DONE | `check:teacher-metric-definitions` source guard + unit export snapshots + authenticated DOM/viewport/export test | Commits `78f4d06a` and `f5d411dc`; `docs/release/artifacts/2026-07-23T20-28-16-105Z/manifest.partial.json` passes 25/25 against commit `f5d411dca9e61a69fd55118ae43348fe6e1e18e4`: zero-warning lint, 998 unit tests, production build, unchanged bundle budgets, reconstructable database schema, deterministic audit seed, live Auth/RLS isolation, every prior teacher/report/export/device regression, the new metric-definition gate, and authenticated accessibility. One canonical registry now defines accuracy, mastered, active, started, current skill, and trails with denominator, date range, minimum evidence, and latest-update semantics. Keyboard-focusable 24px information controls expose the complete definition beside each class, roster, learner, and report figure; the browser gate proves every tooltip stays inside the viewport and cannot block the next teacher action. Excel workbooks carry a `Metric Definitions` sheet and CSV/TXT exports embed the same rows; unit snapshots cover Guided Reading, student/class EL, legacy Excel, legacy CSV/TXT, and the student workspace CSV. The pass also fixed a false `Not started` state where durable Sound Seekers trail progress existed without telemetry sessions, preserved Sound Map context in its definition, restored explicit drawer close focus, and replaced a cramped four-column drawer summary with a reviewed two-column layout. All four Chromebook/tablet baselines were visually reviewed and refreshed without changing the 2.5% tolerance. |
| A6.7 | 6 | P0 | DONE | `check:a11y-teacher` | Commit `2bb12c23`; `docs/release/artifacts/2026-07-23T18-34-38-204Z/manifest.partial.json` passes 20/20 against the committed tree: zero-warning lint, 992 unit tests, production build, unchanged bundle budgets, reconstructable database schema, deterministic audit seed, live Auth/RLS isolation, all prior teacher/report/export regressions, and the authenticated teacher accessibility gate. The seeded journey proves one main landmark, named primary navigation, an eight-column semantic roster, a textual alternative for the class sound map, keyboard activation across all five teacher intentions, and zero serious or critical Axe violations on Today, Classes, Assess, Progress, Plan/Resources, and the class-code dialog. Teacher dialogs now trap forward and reverse focus, close with Escape, and restore focus to the invoking control; two gate-discovered contrast failures in inactive class text and unseen sound-map tiles were fixed at source. |
| A6.8 | 6 | P2 | DONE | `check:teacher-assessment-hub` purpose-language, glossary, learner-selection, and route test | Commits `216a3d5b` and `69620786`; `docs/release/artifacts/2026-07-23T21-01-31-557Z/manifest.partial.json` passes 27/27 against commit `69620786b7b1249f2cda3a1bf58324438e63372a`: zero-warning lint, 998 unit tests, production build, unchanged bundle budgets, reconstructable database schema, deterministic audit seed, live Auth/RLS isolation, every prior teacher/report/export/device regression, authenticated accessibility, the new assessment-hub gate, and the repaired 500-row export-compatibility gate. Assess now exposes one contextual `Assessment hub` instead of overlapping raw product labels. Four teacher purposes — Universal benchmark, Diagnostic follow-up, Progress monitoring, and Practice — have distinct plain-language explanations and verified destinations; the previous terms appear only inside the collapsed language guide. A no-learner visit now offers a working learner chooser instead of disabled dead ends. The critic loop found and fixed an unstable 3+1 desktop grid, unreadable 1024px context truncation, and the frozen main-entry budget overrun; the final layout is a stable responsive 2×2/1-column grid with no horizontal overflow, while purpose routing moved into the lazy hub and the original budget passes with 38 raw bytes and 230 gzip bytes of headroom. The full release audit also exposed A6.6's stale export semantic baseline and lazy-loader assertion; both were updated to the verified Metric Definitions workbooks without weakening the three-snapshot, CSP, 500-row, or memory contracts. |
| A6.9 | 6 | P1 | IN-PROGRESS | `check:teacher-state-matrix` production-reachability gate + shared state primitive | The earlier committed artifact proved the visual 5×8 fixture but overstated its production reachability. D-140 corrected the release claim: the current tree catalogues only eleven combinations selected by concrete Today, Students, Assessments, Reports, and Resources read-state branches; removes generic state-injection props; documents unclaimed states; and makes unsupported combinations fail closed. The shared component retains specific copy, preserved-data language, polite live regions, real handlers, and 44px targets. Final settled-tree unit/build/browser evidence is pending before this row returns to DONE. |
| A6.10 | 6 | P1 | DONE | `check:teacher-ui-primitives` duplication guard + unit contracts + authenticated Chromebook/tablet roster and drawer snapshots | Commit `e22c337c`; `docs/release/artifacts/2026-07-23T21-39-58-683Z/manifest.partial.json` passes 29/29 against commit `e22c337c24e1422519f7221267a51dab9ec90ce0`: zero-warning lint, 1,046/1,046 unit tests, production build, unchanged bundle budgets, reconstructable database schema, deterministic audit seed, live Auth/RLS isolation, teacher/report/export/device regressions, and authenticated accessibility. Teacher tokens, page shells, headers, filters, data tables, charts, modal dialogs, and non-modal drawers now have canonical implementations under `src/components/teacher/ui/`; dashboard and intent surfaces adopt them, destructive admin dialogs share the accessible dialog primitive, and low-frequency destructive UI is lazy-loaded. The grep guard rejects reintroduced local focus traps, page shells, filter regions, table wrappers, and chart roles. Existing Chromebook and tablet roster/drawer baselines pass without snapshot updates or tolerance changes. The critic loop caught an attribute-order-dependent test rather than accepting a false product failure; the assertion now validates the disabled control semantically. Main-entry raw size improved from the frozen 1,148.79 kB budget to 1,146.98 kB (1.80 kB headroom) without changing any budget. |
| A7.1 | 7 | P0 | DONE | route-level teacher-data + product-finish gates | Same 10/10 bounded manifest: the reachable route renders and interacts with scope selection, PDF, Excel generation/save, history, and deletion; the formal-report panel is isolated in its own budget-safe route chunk |
| A7.2 | 7 | P0 | DONE | report release-readiness + 500-item export test | Same 9/9 partial manifest: Skills Check CSV separates Summary from Evidence appendix and includes complete item, attempt, and question rows; storage pagination is covered beyond 5,000 attempts |
| A7.3 | 7 | P1 | DONE | `check:teacher-class-progress` model contracts + authenticated class-to-item-evidence E2E in no more than three clicks | Commits `40285bb8` and `280e349d`; `docs/release/artifacts/2026-07-23T22-13-07-508Z/manifest.partial.json` passes 30/30 against commit `280e349da0805b5767f8657853c93ee5a9a15e13`: zero-warning lint, 1,052/1,052 unit tests, production build, bundle ratchet, reconstructable schema, deterministic seed, live Auth/RLS isolation, all prior teacher/report/export/device regressions, and authenticated accessibility. Progress is now class-first: Distribution separates Secure (85–100%), Developing (70–84%), Needs support (below 70%), and Insufficient evidence (fewer than eight scored responses); Coverage distinguishes any learner evidence, policy-ready evidence, scored responses, and exact Sound Seekers targets; Groups state whether membership comes from a shared curriculum focus or an exact reteach item; Outliers require policy-ready evidence and a distance of at least 15 percentage points from the class median. The seeded path opens Progress, reviews Aisha, and reaches the exact `/m/` item in three clicks, showing 12 recorded encounters, 12 independent attempts, 33% independent accuracy, the saved-item source, and the durable `at-risk` reteach state; reload restores the same class, learner, overview, and honest URL. The critic loop caught and fixed the historical at-risk bucket error and a teacher-route timing race that broke exact student-preview return instead of weakening either assertion. Manual 1366×768, 1024×768, 768×1024, and 390×844 inspections found no horizontal overflow or runtime console errors and retained readable exact-item evidence. The main entry remains within the unchanged budget at 1,147.74 kB raw / 240.47 kB gzip, leaving 1.04 kB raw / 0.40 kB gzip headroom. |
| A7.4 | 7 | P1 | DONE | `check:teacher-evidence-basis` unit model/render contracts + authenticated seeded sparse-learner DOM test | Commit `926c84c29803ba110514c5842f93d853dc641a04`; `docs/release/artifacts/2026-07-23T22-35-35-993Z/manifest.partial.json` passes 31/31 against the committed tree: zero-warning lint, 1,054/1,054 unit tests, production build, bundle ratchet, reconstructable schema, deterministic seed, live Auth/RLS isolation, every prior teacher/report/export/device regression, and authenticated accessibility. Every class distribution, coverage, suggested group, outlier, learner, and exact-item conclusion now exposes attempts, skill diversity, UTC recency, confidence, and captured support use. Accuracy conclusions require eight scored learner responses; exact-item conclusions require three independent attempts; sparse learners are excluded from class averages, reteach groups, and outliers. The seeded one-response learner Amara is reachable from the insufficient-evidence list and shows `Insufficient evidence · 1 of 8 required attempts`, one assessment skill, Jul 2026 recency, and 0 supported of 10 recorded Sound Seekers encounters without rendering a bare `100% accuracy`. The roster, drawer, Progress view, and shared exported metric definition use the same policy; the intentional definition change was propagated through all three semantic workbook snapshots without changing their sheets, rows, cells, lazy CSP-safe loading, or 500-row memory contract. The critic loop rejected an initially bloated 2,473px desktop page, moved repeated group/outlier bases into keyboard-operable disclosures, and produced a 1,465px final 1366px layout; 1366×768, 1024×768, 768×1024, and 390×844 inspections have zero horizontal overflow, with a readable 312px sparse-learner panel on the phone. The tablet roster baseline changed by an explained six vertical pixels after replacing Amara's misleading percentage and was visually reviewed without changing the 2.5% tolerance. Main entry remains inside the unchanged budget at 1,147.84 kB raw / 240.50 kB gzip, leaving 0.95 kB raw / 0.37 kB gzip headroom. |
| A7.5 | 7 | P1 | DONE | `check:teacher-growth-history` five-series model contracts + authenticated paginated-history, curriculum-version, and Axe E2E | Commit `02a6386385b018ecce3e7c5184ae37c73e0dacea`; `docs/release/artifacts/2026-07-23T23-06-54-168Z/manifest.partial.json` passes 32/32 against the committed tree: zero-warning lint, 1,057/1,057 unit tests, production build, bundle ratchet, reconstructable schema, deterministic seed, live Auth/RLS isolation, every prior teacher/report/export/device regression, and authenticated accessibility. Selecting a learner lazily loads complete assessment and reviewed-intervention history in bounded 500-row pages and fails explicitly at a 20,000-row safety ceiling instead of silently truncating; the seeded route proves 524 completed attempts cross the first page and retain all three raw curriculum versions on one dated axis. Five independently selectable evidence views derive skill acquisition, retention, oral-reading fluency, captured support dependence, and reviewed intervention response without storing a competing summary, converting missing evidence to zero, or admitting in-progress attempts and merely recorded outcomes. Every chart has a textual alternative, dated source table, honest loading/error/empty states, 44px controls, and zero serious or critical Axe violations. The critic loop found and fixed a one-row 1024px control squeeze, broken phone labels, colliding curriculum markers, silent intervention pagination, and reviewed partial/ineffective cards falsely labelled effective. Final 1366×768, 1024×768, 768×1024, and 390×844 inspections show readable 5/3/2/1-column metric controls, non-colliding markers, no horizontal overflow, and no console errors. Main entry remains inside the unchanged budget at 1,147.86 kB raw / 240.50 kB gzip, leaving 0.93 kB raw / 0.37 kB gzip headroom; export compatibility still passes three semantic snapshots, CSP-safe lazy loading, 500 rows, and memory ceilings. |
| A7.6 | 7 | P2 | DONE | `check:teacher-instructional-groups` transparent-criteria unit contracts + authenticated create/save/compare/review/assign E2E + live backend ownership/immutability verifier | Commit `c86f4972cd691653b1ca29ca308170f87333f3bd`; `docs/release/artifacts/2026-07-23T23-47-51-706Z/manifest.partial.json` passes 33/33 against the committed tree: zero-warning lint, 1,063/1,063 unit tests, production build, all prior teacher/report/intervention/device regressions, deterministic seed, reconstructable schema, live Auth/RLS isolation, export compatibility, bundle ratchet, and authenticated accessibility. Teachers can save a private instructional group only from an exact transparent suggestion criterion and policy, reload it, select exactly two groups for aggregate comparison, append stayed/joined/left reviews, and assign the latest reviewed membership into the durable Today intervention lifecycle; children are never publicly ranked. Group criteria and review evidence are immutable, latest comparisons use the captured review snapshot rather than drifting with current evidence, malformed membership/evidence rolls back the atomic save, direct table inserts cannot bypass owned RPCs, duplicate/foreign learners and cross-tenant reads/writes/assignments are rejected, and archived groups cannot be reactivated. The critic loop caught and fixed a privileged cleanup cascade deadlock, direct shell/snapshot injection, class-switch transient-state leakage, 40px legacy controls, positional fixture selection, tablet comparison bloat, and historically drifting comparison values without weakening a gate. Reviewed 1366×768, 1024×768, 768×1024, and 390×844 baselines have readable captured-evidence context, no horizontal overflow, 44px controls, zero serious/critical Axe violations, and no console/page errors. Main entry remains inside the unchanged budget at 1,147.86 kB raw / 240.50 kB gzip, leaving 0.93 kB raw / 0.37 kB gzip headroom; export compatibility still passes three semantic snapshots, CSP-safe lazy loading, 500 rows, and memory ceilings. |
| A7.7 | 7 | P1 | DONE | `check:teacher-insight-actions` exact-target/decodability unit contracts + every-card DOM sweep and four-action E2E + live atomicity/ownership/immutability verifier | Commit `0f2e25d10a409e9fdb24f360fa7795ecc379fb59`; `docs/release/artifacts/2026-07-24T00-29-02-696Z/manifest.partial.json` passes 34/34 against the committed tree: zero-warning lint, 1,068/1,068 unit tests, production build, every prior teacher/report/intervention/device regression, deterministic seed, reconstructable schema, live Auth/RLS isolation, export compatibility, bundle ratchet, and authenticated accessibility. Every actionable group, outlier, and exact-item insight exposes visible, enabled Assign practice, Plan small group, Print resource, and Record observation controls. Practice writes one to six exact evidenced targets into the forward-merged Sound Seekers assignment while creating a normal dated intervention; group planning creates the same measurable Today lifecycle; print opens a real decodable practice pack at the lowest evidenced learner stop; direct observation atomically stores an immutable bounded insight snapshot and its next teaching response. The backend rejects malformed/unknown snapshots, duplicate/foreign/archived learners, cross-tenant classes, direct observation inserts, and observation mutation; failed snapshot validation rolls back the linked intervention. The critic loop caught and fixed disabled secure-focus actions, positional seeded assumptions, hidden historic Today counts, an oversized phone action region, 40px modal footer controls, a one-column phone learner list, cancelled-form state leaking into print, label-only snapshots that contradicted their documentation, and a non-idempotent composite-key migration. A pre-existing daily saved-group baseline drift was traced to proportional live-date numerals and fixed without masking or weakening screenshot tolerances. Reviewed 1366×768, 1024×768, 768×1024, and 390×844 cards plus the 390px dialog have no horizontal overflow, 44px actions, zero serious/critical Axe violations, and no console/page errors. Main entry remains inside the unchanged budget at 1,147.88 kB raw / 240.50 kB gzip, leaving 0.91 kB raw / 0.37 kB gzip headroom; export compatibility still passes three semantic snapshots, CSP-safe lazy loading, 500 rows, and memory ceilings. |
| A7.8 | 7 | P2 | DONE | `check:immutable-assessment-evidence` content-independent replay unit contracts + rich persistence coverage + live migration/RLS/immutability verifier | Commit `c90fc2e95b25c0e6ba966756b551a7f1982a6087`; `docs/release/artifacts/2026-07-24T00-58-53-091Z/manifest.partial.json` passes 35/35 against the committed tree: zero-warning lint, 1,072/1,072 unit tests, production build, every prior teacher/report/intervention/device regression, deterministic seed, reconstructable schema, live Auth/RLS isolation, export compatibility, bundle ratchet, and authenticated accessibility. Every normalized result now stores queryable assessment, content, policy, attempt-schema, and evidence-schema versions plus the exact policy snapshot and a bounded replay envelope whose result must equal the stored payload. Adaptive checkpoints capture their round, pass rule, stage, level, and phase; EL benchmarks capture their plan, content, scoring rule, framework, administration range, expected anchor, and suppression state. The database backfills legacy attempts without claiming unknown releases, accepts omitted columns from older clients by deriving an envelope before validation, rejects detached IDs/versions/capture times/results, and freezes completed, discontinued, not-administered, and not-scorable evidence while preserving owned deletion paths. The replay test changes deployed content and policy, reconstructs the old result without consulting current content, and renders the identical report model. The critic loop caught and fixed terminal retries refreshing immutable timestamps, legacy seed incompatibility, incomplete capture-time validation, response-dependent content identities, and a realistic 25-learner/2,500-attempt report exceeding the unchanged conservative browser quota; repeated formal-evidence strings are now dictionary-compressed and losslessly decoded, with workbook provenance verified after reload. Main entry remains inside the unchanged budget at 1,148.36 kB raw / 240.62 kB gzip, leaving 0.42 kB raw / 0.25 kB gzip headroom; export compatibility still passes three semantic snapshots, CSP-safe lazy loading, 500 rows, and memory ceilings. |
| A7.9 | 7 | P1 | DONE | `check:report-export-provenance` unit snapshots for PDF/print, Excel, CSV, and text exports + authenticated teacher route + export compatibility gate | Commit `a309a58a`; `docs/release/artifacts/2026-07-24T03-44-42-502Z/manifest.partial.json` passes 36/36 against the committed implementation: zero-warning lint, 1,079/1,079 unit tests, production build, unchanged bundle budgets, authenticated teacher/report/accessibility journeys, deterministic seed, reconstructable schema, live Auth/RLS isolation, and all prior report/export compatibility gates. Every reachable student, class, assessment, workspace, answer-history, guided-reading, and reading-mastery export now carries the same canonical 16-field provenance block covering school/class/learner identity policy, generated ISO time and timezone, filters, evidence window, exact or deterministic complete-set version identity, definitions, and privacy classification. The critic loop additionally removed the Reading Mastery export's silent 30-row evidence truncation, proved complete first-to-last preservation across 520 rows, retained version summaries in compact saved reports, and moved action-only exporters behind lazy chunks so the main entry remains within the unchanged budget at 1,146.26 kB raw / 239.77 kB gzip. |
| A7.10 | 7 | P2 | DONE | `check:report-audience-templates` | Commit `dd5f323b`; Aarav’s reachable Whole Child report renders a detailed teacher diagnostic, a concise class/leadership summary with coverage and interpretation safeguards, and a strengths-based family update from the same learner record. The complete family-facing main area—not only its cards—passes the enforced jargon, currency, and sentence-length lint and replaces technical provenance/footer copy. `docs/release/artifacts/2026-07-24T05-40-15-266Z/manifest.partial.json`: 5/5 gates and 1,093/1,093 unit tests pass. |
| A8.1 | 8 | P1 | DONE | `check:db-policies` live catalogue + five-actor Auth/RLS/RPC/delete/code/token suite | The final local Docker audit reconstructs all 38 migrations and applies the deterministic two-school seed before interrogating the same live target. The verifier reports 50 public security-definer functions with 13 private helpers, exactly eight anonymous and 37 authenticated RPC signatures, and RLS on all 28 public tables. Anonymous writes and authenticated-only RPCs are denied; Teacher A/B reads and deletes remain tenant-isolated; owned deletion succeeds; expired and rotated codes fail; token and administrator boundaries hold; and leaderboards remain pseudonymous. The added child archive RPC is included in the denial and ownership probes. |
| A8.2 | 8 | P0 + EXTERNAL REVIEW | EXTERNAL-READY | leaderboard DB policy tests + privacy-review readiness | `docs/release/artifacts/2026-07-23T14-22-24-410Z/manifest.partial.json` passes 8/8 (lint, 982 tests, build, bundle and live DB); `docs/release/artifacts/2026-07-23T14-24-53-240Z/manifest.partial.json` passes the reachable teacher privacy control plus invalid-token/anonymous-scope rejection, 12 pseudonymous class rows, explicit teacher opt-in to 25 pseudonymous school rows, and class-scope restoration; independent live review remains open in `EXTERNAL.md` |
| A8.3 | 8 | P1 | DONE | `check:class-access-security` device/network/code burst test + teacher access-log/expiry E2E | The Docker-backed audit re-seeds the school, submits an 11-request abusive burst, proves the real two-minute device lockout, preserves the entered code for recovery, then signs in as Teacher A and verifies the privacy-minimal alert/history plus optional expiry save and clear. The history contract now checks its documented 20-event bound and required generic outcomes rather than an order-dependent fixed count. The focused live rerun passes 2/2 browser journeys and all seven helper contracts; the same gate is required by `check:release`. |
| A8.4 | 8 | P1 | DONE | `check:csp` enforced deployment header + clean startup + six hostile browser probes | Commit `a3e545bdea7bdc7c530830018c459d993857faee`; `docs/release/artifacts/2026-07-25T01-56-45-150Z/manifest.partial.json` passes CSP, zero-warning lint, and all 1,209 unit tests. The built app starts under the exact deployment CSP without `unsafe-eval` or inline script execution; inline script, eval, external script, frame, object, and disallowed connection probes are all blocked. COOP is `same-origin` and CORP is `same-site`. |
| A8.5 | 8 | P0 | DONE | registry-backed dependency audit + export snapshots | The canonical release completes `npm audit --json --audit-level=high` with no high-severity failure. The installed tree excludes the prior ExcelJS archive chain, while export compatibility still passes three semantic snapshots, CSP-safe lazy loading, 500-row serialization/reload, and bounded memory. |
| A8.6 | 8 | P1 | DONE | `check:error-monitoring` redaction contracts + authenticated remote-delivery E2E | The live audit injects a deliberate PII-bearing browser error, verifies redaction and release attribution at ingestion, and confirms the retained event and zero-fatal/repeat-alert budget through the authenticated administrator surface. The canonical release run passes all eight focused contracts and the browser delivery journey; raw names, answers, URLs, account/class/learner IDs, tokens, messages, and arbitrary context remain outside the accepted schema. |
| A8.7 | 8 | P1 | DONE | `check:recovery-drill` isolated PostgreSQL restore + protected-table fingerprints | A real custom-format backup is restored into the explicitly isolated `literacypath_recovery_drill` database. The gate verifies the catalogue and exact counts plus order-independent fingerprints for classes, learners, answers, mastery, item mastery, assessment attempts, and saved EL reports, then writes a secret-free success artifact under `docs/release/artifacts/recovery/`. Same-target, unmarked-target, and unconfirmed destructive runs remain fail-closed. |
| A8.8 | 8 | P1 | DONE | `check:learner-data-rights` seeded admin-export + teacher-deletion E2E | The live Docker audit exports Aarav's complete 520-attempt record, then creates an evidence-bearing disposable learner and completes the verified prepare/confirm/delete workflow. It proves UI and protected-table absence, rejected re-export, cleared initiating-browser state, and the retained privacy-minimal `deletion_completed` tombstone. The JSONB zero-residual defect found by this journey is corrected in a forward migration; eight focused contracts and both authenticated browser journeys pass. |
| A8.9 | 8 | P1 | DONE | `check:retention-policy` bounded policy/job contracts + admin reachability + propagation evidence rules + synced public/legal/runbook disclosure | Commit `ad2e79c7ce5169b4e9935f853bfbb992ee0395d3`; `docs/release/artifacts/2026-07-25T04-20-05-544Z/manifest.partial.json` passes 6/6 against the exact committed tree: zero-warning lint, all 1,244 unit tests, production build, reconstructable migration order, the focused retention gate, and the counsel-pack/public-policy sync gate. Admin → Schools now exposes per-school inactivity archive, delete-after-archive, year-end archive/delete, provider target, and backup target controls with bounded values, a no-change preview, exact destructive confirmation, database-clock eligibility, the existing zero-residual data-rights deletion transaction, immutable policy/preview/result snapshots, and initiating-browser cache/queue/report cleanup. Deletion creates a privacy-minimal propagation record; elapsed target dates become `evidence_required` and can never self-certify deletion. Verification requires both dates to pass, a retrievable evidence reference, and exact confirmation. The public page candidly names current providers while leaving deployment regions, contractual periods, production scheduling, and actual provider/backup evidence unconfirmed for operational/external completion; product defaults are explicitly not legal conclusions. |
| A8.10 | 8 | P0 EXTERNAL | EXTERNAL-READY | `check:legal-pack` complete-pack, public-policy sync, and false-approval guard | Implementation commit `6d77c791` adds the 12-document counsel pack covering draft service terms, Article 28-style DPA annexes, a candid provider/location/transfer register, security boundaries, WCAG 2.2 target and limitations, exercised-incident requirements, school direct notice and authorisation, parent notice/consent, learner assent, rights intake, COPPA/FERPA/PPRA/UK/EU matrix, deployment-facts register, and an entirely unchecked counsel sign-off record. It also reconciles the public privacy page with actual browser storage, accessibility/support settings, operational events, authoring-only AI separation, Google Fonts requests, and unresolved retention/transfer facts. `docs/release/artifacts/2026-07-25T03-21-54-118Z/manifest.partial.json` passes 4/4 against the exact implementation commit: zero-warning lint, 1,228/1,228 tests, production build, and the legal-pack gate. The gate fails if the DPA is missing, legal approval is fabricated, or a counsel box is self-checked. Qualified external legal review, resolved launch facts, and executed terms remain open in `EXTERNAL.md`; no legal approval or compliance certification is claimed. |
| A9.1 | 9 | P1 | DONE | enforced 500,000-byte raw / 150,000-byte gzip main-entry budget + analysed static-graph guard | Commit `62934d5f` removes the teacher-only EL benchmark catalog, session builder, and scorer from the initial static graph behind one retryable dynamic engine boundary, with an honest preparing state and duplicate-start guard. The final main-entry ratchet is active immediately and no later schedule step can loosen it. `docs/release/artifacts/2026-07-25T06-52-35-427Z/manifest.partial.json` passes 6/6 against the exact commit: zero-warning lint, all 1,263 unit tests, production build, 12 desktop/mobile smoke journeys, the final bundle ceiling, and the analysed split-boundary guard. Main entry is 452.79 kB raw / 120.39 kB gzip, leaving 47.21 kB / 29.61 kB enforced headroom. |
| A9.2 | 9 | P1 | DONE | `check:split-boundaries` ≤700 kB route/data ceiling + `check:first-load-network` production traffic proof | Commit `599949a2` converts five 3,654-question audit-sized barrels into deterministic per-skill/level sources and makes the assessment loader fetch only the requested skill's shards while preserving every full array, source order, normalized candidate, and count. Guided-reading, child-media, Phaser, Quest, choice-audio, and the formal assessment hub now have bounded lazy ownership; no `CHECK-CHANGE` approval or budget increase was used. `docs/release/artifacts/2026-07-25T07-30-01-030Z/manifest.partial.json` passes 7/7 against the exact commit: zero-warning lint, all 1,264 tests, production build, 12 desktop/mobile smoke journeys, bundle budgets, an analysed graph with 52 dynamic runtime shards and every route/data chunk ≤700,000 bytes, plus fresh student/teacher production network logs with zero deferred bank, guided-reading, benchmark-content, export, 3D, or game-engine requests. |
| A9.3 | 9 | P1 | DONE | `check:split-boundaries` zero-warning build + analysed module/chunk guard | Commit `d2028b2e` replaces the browser's static dependency on the build-time master lexicon with a deterministic 1,217-row media-only projection whose complete equality is unit-tested. CVC, Initial Sounds, IXL seed, and Rhyming banks now each emit as a true dynamic entry; the 1 MB build-time lexicon is absent from every browser chunk. Commit `62934d5f` extends the permanent analysed guard to traverse the entry's static chunk graph and reject any EL benchmark engine, catalog, session, or scorer leak. `docs/release/artifacts/2026-07-25T06-52-35-427Z/manifest.partial.json` passes the extended guard with zero ineffective dynamic imports alongside lint, 1,263 tests, build, smoke, and the final main-entry ratchet. |
| A9.4 | 9 | P1 | DONE | eval-free build + 500-item export memory test | Commit `20e16efc`; `docs/release/artifacts/2026-07-23T14-53-41-215Z/manifest.partial.json` passes 6/6: zero-warning lint, 982 tests, production build without the ExcelJS eval warning, authenticated `.xlsx` download, bundle ratchet, three semantic workbook snapshots, lazy chunk assertion, and 500-row serialize/reload within the 160 MiB heap / 224 MiB RSS ceilings. |
| A9.5 | 9 | P1 | DONE | `check:app-decomposition` 3,000-line ratchet + six explicit controller/service/rendering boundaries + full tests | Milestone commits `a681bd56`, `2ca3bc53`, and `ef99612079f095f17292616530ae24d0da17bc8c` reduce `App.jsx` from 9,716 to 6,937, then 4,582, then 2,311 lines without a big-bang rewrite. Assessment runtime policy, runtime loading/surfaces, session and teacher workspace orchestration, assessment-round selection/persistence/feedback, and final guarded rendering now have explicit owners. The permanent release gate fails above 3,000 lines or if any of six required boundaries disappears. Final exact-commit manifest `docs/release/artifacts/2026-07-25T06-40-43-443Z/manifest.partial.json` passes 6/6: zero-warning lint, all 1,262 unit tests, production build, 12 desktop/mobile student/teacher smoke journeys, the final decomposition ratchet, and every unchanged bundle budget. The one-byte child-assets compression regression found during the final critic loop was fixed with a smaller equivalent QA lookup rather than a waiver. The follow-on A9.1 split at `62934d5f` now also brings the entry below its final 500/150 target. |
| A9.6 | 9 | P0 | DONE | dashboard consolidation + reachable route contracts | Same 10/10 partial manifest: live teacher, product-finish, high-volume report, build, bundle, and audit-school isolation gates all pass |
| A9.7 | 9 | P2 | DONE | `check:teacher-routing` RLS-validated class/report deep link + refresh/Back E2E; `check:el-assessment-route-final-review` session/item resume | Canonical teacher URLs preserve class, child and report identity, and the redesigned Reports surface opens the whole-child report directly. The final authenticated local gate passes all 14 route contracts plus two browser journeys: Teacher A's Skills report survives a hard refresh, changes to Overview, and returns to the exact Skills URL with browser Back; Teacher B cannot list Audit Class A or expose its child/report through the same deep link. |
| A9.8 | 9 | P1 | DONE | `check:domain-boundaries` typed/runtime boundaries + alias-independent private SDK call-site guard | Commit `804d0c72` introduced five explicit auth, class, evidence, report, and content registries for all 16 tables and 36 RPCs. D-034 commit `ffce41e3` then migrated the remaining aliased `client.rpc` callers and strengthened the guard so any application receiver using direct `.rpc()`/`.from()` fails, including regression fixtures for alternate receiver names. Successful response envelopes and versioned stored payloads remain runtime-validated, learner roster/login identity payloads fail closed, and the raw SDK client remains private. `docs/release/artifacts/2026-07-25T08-12-21-575Z/manifest.partial.json` passes the exact implementation commit. |
| A9.9 | 9 | P1 | DONE | `check:private-source-maps` + `check:public-source-maps` + `check:error-monitoring` | Normal builds expose zero maps or map references; the restricted build maps every source-bearing production chunk and resolves a real minified frame to its source. The hydrated live audit now also completes deliberate redacted error → retained release event → authenticated administrator dashboard and enforces the documented rolling budget of zero fatal or repeat-alert events. No telemetry is still labelled unverified rather than healthy, and no availability percentage is inferred from sampled diagnostics. |
| A9.10 | 9 | P1 | DONE | `check:sync-chaos` two-context durable queue and forward-only merge browser suite | Commit `6701de5d13cb9f5bbf91edd72114f199b1511151`; `docs/release/artifacts/2026-07-25T02-05-27-251Z/manifest.partial.json` passes sync chaos, zero-warning lint, and all 1,210 unit tests. Two independent browser contexts prove offline save, reload persistence, stale-version conflict handling, token expiry, bounded retry, visible recovery metrics, and no lost stops. |
| A10.1 | 10 | P0 | IN-PROGRESS | required whole-product CI release job | `release-gate` job implemented with fresh DB, audit seed, manifest artifact, and enforced result; hosted CI + branch-protection evidence pending |
| A10.2 | 10 | P0 | DONE | `check:release` manifest + generated scorecard | The canonical runner executes all application, curriculum, accessibility, browser, database, security, recovery, reporting, performance, and media gates against one locked local audit target and writes `docs/release/manifest.json` plus the generated scorecard. Preflight rejects missing or mismatched frontend/audit targets so browser and SQL evidence cannot silently describe different systems. |
| A10.3 | 10 | P1 | DONE | zero-warning lint gate | CI invokes `npm run lint -- --max-warnings=0`; `docs/release/artifacts/2026-07-23T12-46-12-124Z/manifest.partial.json` proves zero warnings with focused route and lifecycle regressions |
| A10.4 | 10 | P1 | IN-PROGRESS | enforced bundle budgets + PR trend table | D-017 implementation restored every unchanged ratchet; commit `62934d5f` reached and activated the final 500 kB raw / 150 kB gzip entry ceiling, and commit `599949a2` completes the staged ≤700 kB route/data target without approvals or rebaselining. `docs/release/artifacts/2026-07-25T07-30-01-030Z/manifest.partial.json` passes bundle, analysed split, and production first-load traffic gates alongside lint, 1,264 tests, build, and smoke. A green credentialed whole-product release and hosted PR-summary evidence remain open. |
| A10.5 | 10 | P2 | DONE | zero-failure baselined repo hygiene | `docs/release/artifacts/2026-07-23T12-20-36-943Z/manifest.partial.json`: 0 failures; explicit preview/generated allowlist in `tools/hygiene-baseline.json` |
| A10.6 | 10 | P2 | DONE | read-only audit check-mode guard | Same manifest passes OS-constrained `--check` write redirection; 23 report commands have paired check modes and canonical tracked reports remain unchanged |
| A10.7 | 10 | P1 | DONE | `check:e2e-teacher` sequential authenticated ownership/report/export journey | The hydrated one-worker journey now completes Teacher A sign-in → owned class → Aarav → current Checks surface → direct Skills report → parsed 520-attempt export, the full EL final-review path, all teacher recovery states, and Teacher B ownership denial. It also exercises the redesigned Dashboard, Children, Checks, Reports, Resources, and Settings workflow against the real seeded backend. |
| A10.8 | 10 | P0 + EXTERNAL MANUAL AUDIT | EXTERNAL-READY | `check:external-program-readiness` + `check:a11y-routes` + `check:a11y-teacher` | Commit `ffce41e3` adds a human-readable inventory mirroring all 17 executable primary routes, 7 key modal/overlay states, and both 1280×900/390×844 automated viewports. `docs/accessibility/MANUAL_AUDIT_PROGRAM.md` defines independent roles, screen-reader, switch/keyboard-only, 200% zoom, audio-off, motion-off, and child-usability execution, dated target windows, stop rules, and retest/closure evidence. `docs/release/EXTERNAL.md` contains an entirely unchecked first-run record and explicitly states that no auditor, school, or child work has started. `docs/release/artifacts/2026-07-25T08-12-21-575Z/manifest.partial.json` passes 48/48 automated route/state cases inside a 6/6 exact-commit run; human execution remains external and is not claimed. |
| A10.9 | 10 | P1 | DONE | `check:curriculum-board` live admin content-QA route + Loop D board identity | The authenticated administrator route and `docs/release/CURRICULUM_BOARD.md` render from the same generated source: all 30 skills are READY with zero blocked skills, strict/status/exposure/loader parity, canonical ownership, approved/exposed counts, fingerprints, and review-dated exclusions. The live route assertion, generator, model, exact loader, and 500-session-per-skill simulation all pass and fail closed on drift. |
| A10.10 | 10 | P1 EXTERNAL | EXTERNAL-READY | `check:external-program-readiness` + `check:research-pilot-pack` | Commit `ffce41e3` adds `docs/research/RECURRING_OBSERVATION_PROGRAM.md`, turning the A1.10 one-off pack into launch, monthly-first-quarter, quarterly, and material-change cycles. It specifies representative teachers from multiple schools; ages 5–10; emergent, early, and transitional readers; multilingual learners; accessibility/assistive-technology needs; supported devices/modes; consent/assent, privacy, stop, small-cell, and required-record rules; and a finding pipeline that reopens traceability, appends DISCOVERED, creates a failing acceptance criterion, and converts mechanizable critical/major findings into permanent release gates with independent retest. The exact 6/6 manifest passes the 11-artifact research-pack contract; recruitment and sessions remain `not_started`, with no human result claimed. |

## Discovered findings

These rows are mechanically consumed by the scorecard. Each ID also has a full evidence, fix, and gate specification in `DISCOVERED.md`.

| Item | Area | Priority | Status | Named gate | Evidence |
|---|---:|---|---|---|---|
| D-001 | 7, 8 | P0 | DONE | `check:el-export-consistency` | Commits `70d1be5d` + `64634b87`; shared canonical evidence read, production-shaped Bao Skills Check seed, and authenticated parsed CSV/XLSX comparison prove matching letter evidence/statuses, source/read/sync provenance, and no duplicate case-less fallback rows. `docs/release/artifacts/2026-07-24T04-26-55-008Z/manifest.partial.json`: 37/37 gates and 1,083/1,083 unit tests pass. |
| D-002 | 7 | P0 | DONE | `check:el-export-consistency` | Commits `70d1be5d` + `64634b87`; grade/window are required before export, zero saved EL evidence opens an explicit accessible warning, filenames remain clean, and authenticated workbook parsing proves exactly three banner/provenance/definitions sheets with no zero-filled assessment rows. Same 37/37 manifest. |
| D-003 | 4, 7 | P1 | DONE | `check:whole-child-evidence-integrity` | Commit `959270a3`; Sound Seekers retains each sound's real mastery `lastAt`, exports distinguish last evidence from an undated snapshot, UTC timestamps use one ISO format, and teacher-local time names the resolved timezone. `docs/release/artifacts/2026-07-24T05-03-16-868Z/manifest.partial.json`: 5/5 gates and 1,087/1,087 unit tests pass. |
| D-004 | 1, 4, 7 | P1 | DONE | `check:whole-child-evidence-integrity` | Commit `959270a3`; related letter-sound/practice judgments carry explicit triangulation notes without averaging evidence strength, every status row exports attempts and an evidence window, curriculum gaps are visible as Not checked/Not seen, PA uses canonical phoneme keys (`c`/`k` → `/k/`, `q` → `/kw/`), every literacy area renders with an explicit no-data state, and the five-status vocabulary is enforced. Same 5/5 manifest. |
| D-005 | 4, 6 | P1 | DONE | `check:el-assessment-item-copy` + `check:el-assessment-completion-device-matrix` | Commit `e322df2e`; every PA strand/task plus Encoding, Decoding, and Fluency renders a two-line task heading and exact imperative teacher direction above the stimulus at 17–21px, while protected scripts retain explicit “Say exactly” treatment. Authenticated eight-item completion remains usable across the four-device matrix. `docs/release/artifacts/2026-07-24T05-11-41-548Z/manifest.partial.json`: 6/6 gates and 1,089/1,089 unit tests pass. |
| D-006 | 3, 4, 6 | P0 | DONE | `check:el-assessment-completion-device-matrix` | Commit `96be1f64`; EL runner outer overflow no longer clips, the sticky bar keeps progress + Save & exit + Finish reachable, and the final placement scroll target is fully usable. Authenticated Amara journeys complete and persist all 8 items at 1280×720, 1366×768, iPad landscape, and iPad portrait; four reviewed screenshot baselines guard the layout. `docs/release/artifacts/2026-07-24T04-47-27-501Z/manifest.partial.json`: 6/6 bounded gates and 1,084/1,084 unit tests pass. |
| D-007 | 6 | P1 | DONE | `check:el-assessment-item-copy` | Commit `e322df2e`; item and route headers compose plain copy such as “Beginning of year · starting band: Middle Pre,” expected ranges are built from named bands, and rendered-DOM/source guards reject raw `BOY:`, `MOY:`, `EOY:`, “Kindergarten BOY,” and grade-window jargon. Same 6/6 manifest. |
| D-008 | 9 | P2 | DONE | `check:el-assessment-route-final-review` | Commit `720b0762`; the live benchmark hash names learner, assessment, session, and exact item, a browser refresh on item 4 restores item 4 with the first three responses intact, and completion returns to a non-stale assessment-hub URL. `docs/release/artifacts/2026-07-24T05-26-39-203Z/manifest.partial.json`: 6/6 gates and 1,091/1,091 unit tests pass. |
| D-009 | 4 | P2 | DONE | `check:el-assessment-route-final-review` + `check:el-assessment-completion-device-matrix` | Commit `720b0762`; Finish opens an explicit scored/skipped/not-scorable tally, cannot archive without confirmation, and “Change final answer” returns to item 8 so a mis-tap can be corrected before the placement is re-confirmed. The authenticated journey and four supported device sizes pass in the same 6/6 manifest. |
| D-010 | 8, 10 | P0 | DONE | `check:database-bootstrap-schema` + fresh local Supabase reset | Fresh local reset applied all 20 migrations; canonical `docs/release/manifest.json` passes schema reconstruction, deterministic seed, live Auth, fixtures, and RLS isolation |
| D-011 | 9 | P2 | DONE | reachable teacher dashboard/report Playwright journey | Zero `pageerror` events; `docs/release/artifacts/2026-07-23T11-59-30-807Z/manifest.partial.json` passes all three authenticated route gates |
| D-012 | 10 | P1 | DONE | canonical-env `test:smoke` preview-route journey | Same partial manifest passes 10/10 desktop/mobile smoke tests against the maintained `/preview/` entry points under canonical Supabase configuration |
| D-013 | 4, 7 | P0 | DONE | reachable 520-item report/export journey | Same 9/9 partial manifest: learner selection hydrates and merges complete cloud history instead of replacing it with the 400-row browser cache |
| D-014 | 7, 10 | P0 | DONE | SQL item-count assertion + reachable export journey | Same 9/9 partial manifest: audit seed has 520 production-readable `questionRecords`; signed-in CSV proves 520 unique item IDs |
| D-015 | 6, 7 | P1 | DONE | complete-evidence loading state + authenticated route test | `docs/release/artifacts/2026-07-23T13-27-33-171Z/manifest.partial.json`: report choices remain disabled until selected-learner hydration finishes; 520-item route export passes in the bounded release run |
| D-016 | 4, 6, 7, 10 | P0 | DONE | production-shaped audit seed + generic-category unit + authenticated report routes | Same 10/10 bounded manifest: canonical seed guard passes, generic legacy identity resolution is unit-covered within 979 passing tests, both report route gates pass, and bundle budgets remain green |
| D-017 | 9, 10 | P0 | DONE | `check:bundle-size` + full `check:release` | The final 500,000/150,000-byte main-entry ratchet, ≤700,000-byte route/data ceiling, analysed split graph and fresh-browser network proof all pass inside the credentialed whole-product release without waivers or rebaselining. |
| D-018 | 3, 10 | P1 | DONE | `check:a11y-routes` | Commit `0354c7ad`; `docs/release/artifacts/2026-07-25T01-35-16-127Z/manifest.partial.json` passes all 48 desktop/mobile route and key-state accessibility cases, including both reachable Adventure Map views, against the exact committed tree. |
| D-019 | 1, 4, 10 | P0 | DONE | `check:assessment-runtime-variation` + `check:assessment-media-evidence` | Commit `0354c7ad`; the same 12/12 manifest proves zero failures for all 16 audited priority skills, 1,206/1,206 unit tests including explicit release-audio registry cases, and reachable failed-media removal/refill without approving the unreviewed K3 inventory. |
| D-020 | 8, 10 | P0 | DONE | online dependency audit + export/build/bundle compatibility | The canonical registry-backed audit now runs successfully with the full release and reports no high-severity failure. Archiver 8/Unzipper 0.12 compatibility, semantic export snapshots, the 500-row memory case, build, bundle budgets, and repository hygiene all remain green. |
| D-021 | 2, 3, 10 | P1 | DONE | `check:student-emphasis-budget` + `check:key-route-visuals` + `check:device-matrix` | Commit `0354c7ad`; the same manifest passes 18/18 emphasis views, 15/15 key-route/delayed-load cases, and 12/12 device journeys after individual review of every changed baseline and repair of the clipped Guided Reading explanation. |
| D-022 | 8, 9, 10 | P0 | DONE | `check:e2e-teacher` + `check:csp` + `check:sync-chaos` | The hydrated local release blocks all hostile CSP probes, passes the two-context offline/stale/token-expiry sync suite, and completes Teacher A ownership/report/export plus Teacher B denial against the deterministic audit school. |
| D-023 | 10 | P0 | DONE | audit-target preflight + hydrated full release | Preflight requires and locks matching frontend and audit targets without printing secret values. The Docker-backed authenticated release uses that one target for seed, browser, SQL, policy, recovery, and data-rights evidence and writes the canonical manifest. |
| D-024 | 8, 10 | P1 | DONE | `check:learner-data-rights` | Shared teacher/admin export and exact-confirmation deletion, non-cascading privacy-minimal tombstones, request history, local cleanup, and zero-residual verification now pass the destructive authenticated browser journey: export → delete → absence → rejected re-export → retained audit proof. |
| D-025 | 8, 10 | P0 | DONE | `check:retention-policy` | Commit `ad2e79c7ce5169b4e9935f853bfbb992ee0395d3`; database time is authoritative, elapsed provider/backup targets require evidence instead of self-certifying, exact verification confirmation is enforced, and initiating-browser learner cleanup is wired. `docs/release/artifacts/2026-07-25T04-20-05-544Z/manifest.partial.json` passes 6/6 and all 1,244 tests. |
| D-026 | 8, 10 | P0 | DONE | `check:db-policies` | A clean local Docker reconstruction applies all 38 migrations and the deterministic audit seed, then the live verifier passes the complete catalogue and actor exercise: 50 security-definer functions, 13 private helpers, exactly eight anonymous and 37 authenticated RPCs, 28 RLS tables, anonymous denial, Teacher A/B isolation, owned and cross-tenant deletion, expired/rotated codes, token scope, admin boundaries and pseudonymous leaderboards. |
| D-027 | 9, 10 | P1 | DONE | `check:teacher-routing` | Canonical teacher/report URLs restore through RLS-validated class and child loads, invalid context fails safely, and report tabs preserve class/child identity. The final local gate passes 14 route contracts, Teacher A refresh/Back restoration, and Teacher B denial against the clean audit backend. |
| D-028 | 4, 8, 9, 10 | P1 | DONE | `check:domain-boundaries` | Commit `804d0c72`; scattered direct SDK queries were replaced with the registered boundary API, the raw client is private, successful auth/table/RPC responses are validated by domain, and malformed nested learner identities plus versioned evidence fail closed. `docs/release/artifacts/2026-07-25T05-33-26-496Z/manifest.partial.json` passes 6/6, including all 1,260 tests and 12 smoke journeys. |
| D-029 | 4, 9, 10 | P1 | DONE | `check:app-decomposition` | Milestone commits `a681bd56`, `2ca3bc53`, and `ef996120` reduce `App.jsx` from 9,716 lines to 2,311 behind six explicit owners; `docs/release/artifacts/2026-07-25T06-40-43-443Z/manifest.partial.json` passes the final 3,000-line ratchet, all 1,262 tests, build, smoke, and bundle checks. |
| D-030 | 9, 10 | P1 | DONE | `check:bundle-size` | Commit `ef996120` removes a redundant blocked-asset lookup allocation rather than changing the budget; the exact A9.5 manifest records `childAssets` below both frozen ceilings. |
| D-031 | 9, 10 | P1 | DONE | `check:bundle-size` + `check:split-boundaries` | Commit `62934d5f` lazily isolates the formal benchmark engine and permanently activates the final 500,000/150,000-byte entry ceiling. `docs/release/artifacts/2026-07-25T06-52-35-427Z/manifest.partial.json` passes 6/6 with entry at 452.79/120.39 kB, 1,263 unit tests, 12 smoke journeys, and an analysed no-static-leak guard. |
| D-032 | 9, 10 | P1 | DONE | `check:split-boundaries` + `check:first-load-network` | Commit `599949a2` replaces monolithic runtime banks with byte-equivalent per-skill/level shards, bounds guided-reading/child-media/Phaser/Quest ownership, removes the full audio-manifest lookup, and lazy-loads the formal assessment hub. `docs/release/artifacts/2026-07-25T07-30-01-030Z/manifest.partial.json` passes 7/7 with 52 dynamic shards, every route/data chunk ≤700 kB, and zero heavy/deferred requests on fresh production student and teacher shells. |
| D-033 | 9, 10 | P1 | DONE | `check:private-source-maps` + `check:public-source-maps` + `check:error-monitoring` | The public build contains zero maps/references, the restricted maps cover every source-bearing chunk and symbolicate a real frame, and the hydrated browser journey proves deliberate redacted error → retained release event → authenticated administrator dashboard under the zero-fatal/zero-repeat-alert budget. |
| D-034 | 3, 8, 9, 10 | P0 | DONE | `check:domain-boundaries` + `check:external-program-readiness` | Commit `ffce41e3`; every remaining aliased direct RPC caller now uses the registered response-validated boundary, the source guard detects arbitrary receiver names instead of only `supabase`, focused boundary/service contracts pass, and the repaired teacher previews complete all 48 desktop/mobile accessibility cases without page errors. `docs/release/artifacts/2026-07-25T08-12-21-575Z/manifest.partial.json` passes 6/6 against that exact tree. |
| D-035 | 1, 4, 10 | P0 | DONE | `check:curriculum-release-standard` + `check:curriculum-board` | Commit `2036416b`; every READY skill exposes only its exact canonical question-and-level set, while version, count, or missing-ID drift fails closed. The three strict/runtime mismatches are visibly BLOCKED with zero child exposure, and the admin/Loop D source records exact fingerprints and reasons. The exact 6/6 manifest proves zero strict/status/exposure/loader mismatches; live admin rendering remains tracked separately by A1.6/A10.9. |
| D-036 | 9, 10 | P1 | DONE | `check:bundle-size` + `check:split-boundaries` | Commit `2036416b`; the exact exposure implementation briefly exceeded the unchanged QuestHub gzip ratchet by two bytes. Redundant Three.js default material properties were removed without changing rendering. The exact 6/6 manifest reports QuestHub at 140.26 kB gzip beneath the original 140.28 kB displayed ceiling and keeps every split boundary green, with no waiver or rebaseline. |
| D-037 | 1, 4, 10 | P0 | DONE | strict audit + `check:curriculum-release-standard` | Commit `ebe0ca16` makes canonical `formatType` authoritative, applies the same release filter to every skill, repairs homophone meaning answers, and admits only individually reviewed Level 1 targets. The exact 12/12 manifest reports 30/30 production-ready, zero question gaps, and zero strict/status/exposure/loader mismatches. |
| D-038 | 1, 9, 10 | P0 | DONE | production build + `check:split-boundaries` | Commit `ebe0ca16` gives the loader and generator one required-shard contract and preserves valid empty modules. Regeneration followed immediately by the production build succeeds; the exact manifest retains 52 dynamic skill/level shards and zero ineffective imports. |
| D-039 | 1, 4, 10 | P0 | DONE | `check:assessment-runtime-variation` + `check:runtime-variation-simulation` | Commit `ebe0ca16` moves both audits onto exact published child banks and canonical per-format media roles, while replacing repetitive preposition prompts and unrelated distractors with specific meaning clues and spatial alternatives. The exact manifest records zero short-audit failures and 15,000/15,000 simulated sessions without a failure. |
| D-040 | 1, 4, 10 | P1 | DONE | strict audit + selector contracts + `check:runtime-variation-simulation` | Commit `ebe0ca16` uses stable phonics-pattern identities, measures distinct targets rather than duplicate variants, adds only reviewed vowel-team targets, balances plural rules across `s`, `es`, `ies`, and `ves`, and expands real distractor/phase variety. All 30 skills remain READY and the required 500 sessions per skill pass. |
| D-041 | 1, 4, 10 | P1 | DONE | `check:assessment-skill-contracts` | Implementation commits `7fd96ddf` and `b09135e7` replace the stale 21-skill sentinel map with the canonical 30-skill release exposure, generated HFW bands, live phase semantics, and the production selector. `docs/release/artifacts/2026-07-25T11-03-32-966Z/manifest.partial.json` passes 14/14 against exact commit `b09135e7c17e84bbec099aa86d7548a2d4e48aa9`: all 30/30 contracts match the exact published child IDs, all 120/120 initial phase rounds and 120/120 wrong-answer retry rounds retain 15 selectable questions, and the 15,000-session runtime simulation has zero failures. The permanent contract gate is now part of the depth, variation, and runtime-selectability dimensions. |
| D-042 | 1, 9, 10 | P1 | DONE | `check:media-quality` + `check:media-runtime-resolution` | Commit `ebe0ca16` replaces the undeclared Python/PIL dependency with installed Sharp pixel analysis and excludes blocked candidates from the shared runtime source registry without deleting their audit history. The exact `docs/release/artifacts/2026-07-25T11-03-32-966Z/manifest.partial.json` resolves 2,410/2,410 built-app URLs with zero wiring, image, or audio failures; the pixel/semantic gate scans 2,183 public images and 12,093 active mappings with zero known-unsuitable, excluded-target, or semantic-conflict failures, while retaining 25 explicit release exclusions. |
| D-043 | 1, 3, 4, 10 | P0 | DONE | `check:assessment-skill-contracts` + `check:media-quality` + `check:media-runtime-resolution` | Commit `7fd96ddf` makes effective answer-option images follow the canonical reviewed image cards and replaces the vowel-team lighthouse face illustration with the approved `light` target, so a question cannot pass media QA through one field while rendering a blocked duplicate through another. The exact 14/14 manifest at `docs/release/artifacts/2026-07-25T11-03-32-966Z/manifest.partial.json` proves the corrected questions remain in their exact published banks, all 2,410 built media URLs resolve, and zero blocked or semantically conflicting mappings are active. Commit `b09135e7` regenerates the authoritative media board from that same live model instead of accepting stale counts. |
| D-044 | 3, 4, 10 | P1 | DONE | `check:calibration-readiness` | Commit `4e5e4b46ac9f8d445c1956f4e9893fe6a9bb988b` enforces a 14-day contradictory-follow-up window, rejects direct identity fields and duplicate item events, and suppresses both matched-group counts if either cell is below five. The desktop/mobile browser critic also repaired below-AA label contrast and made all independently scrollable result tables descriptively keyboard-focusable. Exact manifest `docs/release/artifacts/2026-07-25T11-38-10-170Z/manifest.partial.json` passes nine focused model/source contracts, all 1,288 unit tests, and both production-component browser journeys with zero overflow, serious/critical Axe violations, console errors, or page errors. |
| D-045 | 1, 4, 10 | P0 | DONE | `check:curriculum-composed` | Commit `25dac6da110e899b2c75afcc93bb89451681a87c` accepts the synthetic composed selection, deterministically expands it to all nine dependency gates, records the same five-dimensional per-skill result used by the credentialed full release, and exposes one stable package command. Exact manifest `docs/release/artifacts/2026-07-25T11-58-03-296Z/manifest.partial.json` records the synthetic result itself as gate ten, with correctness, depth, variation, media, and runtime-selectability all passing globally and for 30/30 skills. |
| D-046 | 6, 9, 10 | P0 | DONE | teacher dashboard no-class route contract + unit suite | Commit `50098bee` requires both a real access summary and a selected class before class-scoped data is read, preserving the honest no-class Today state instead of entering the global error boundary. |
| D-047 | 5, 8, 9, 10 | P0 | DONE | `check:domain-boundaries` + instructional-group contracts | Commit `50098bee` registers both durable instructional-group tables in the private data boundary while retaining response validation and the raw-client ban. |
| D-048 | 5, 8, 9, 10 | P0 | DONE | migration reconstruction + class creation contracts | Commit `50098bee` repairs the private class-code trigger with a fixed-search-path security-definer boundary; onboarding, demo and roster class creation retain non-browser generator permissions. |
| D-049 | 5, 6, 9, 10 | P0 | DONE | teacher route restoration contracts | Commit `50098bee` marks the teacher profile unresolved before restoration can apply a fallback, so valid Classes and Progress deep links survive hard reload. |
| D-050 | 4, 7, 10 | P0 | DONE | deterministic audit-seed contracts | Commit `50098bee` clears legacy teacher assessment/report rows before rebuilding the fixture and asserts the canonical long-history and formal-result counts. |
| D-051 | 8, 10 | P1 | DONE | database-policy invocation contract | Commit `50098bee` passes the approved database URL directly to the policy verifier and proves ambient PostgreSQL settings cannot replace it. |
| D-052 | 2, 6, 8, 9, 10 | P0 | DONE | rolling-schema class API compatibility contracts | Commits `fdbf2259` and `a199dfab` restore classes and child rosters against the hosted previous schema while keeping the fully migrated calls first, limiting fallback to exact missing-column/missing-overload errors, and passing unrelated authorization/network failures through unchanged. |
| D-053 | 1, 2, 4, 10 | P0 | DONE | letter-shape scoring contracts + reachable child browser review | Commit `8aedef3b` replaces fill coverage with explicit uppercase/lowercase stroke geometry. Correct paths pass; boxes, X shapes, off-path scribbles, reversed/incomplete strokes and partial coverage fail. The real Adventure Map Letter Trace activity was opened in the browser after the fix. |
| D-054 | 1, 2, 3, 5, 6, 7, 10 | P1 | DONE | `check:app-copy` source + complete rendered DOM + export output | Commit `7fd3b9ac` applies the app-wide audience vocabulary and adds a permanent release gate over eight teacher routes, nine child routes, closed detail content, accessible names, child line length/fractions, and actual Excel/CSV output. Exact manifest `docs/release/artifacts/2026-07-25T17-12-38-979Z/manifest.partial.json` passes 1/1: 17/17 rendered routes and 2/2 export contracts. Zero-warning lint, all 1,303 unit tests, production build and unchanged bundle budgets also pass locally. |
| D-055 | 5, 8, 9, 10 | P0 | DONE | child-lifecycle unit, live-policy and authenticated browser contracts | Owned rename/archive RPCs, active and archived roster views, focused Edit/Archive/Restore actions and separate permanent privacy deletion are implemented. The authenticated journey proves rename, verified privacy-export readiness/history, archive, archived count, restore and name restoration; the live verifier covers RPC denial and tenant isolation. |
| D-056 | 6, 8, 9 | P1 | DONE | learner-data-rights dialog unit + authenticated lifecycle journey | Privacy history now fails and retries independently of authorised export/deletion actions, so a history outage cannot disable or misreport another operation. |
| D-057 | 2, 5, 6, 7, 9 | P1 | DONE | authenticated six-section IA + current teacher workflow contracts | Dashboard, Children, Checks, Reports, Resources and Settings now form the complete teacher navigation. School information, Site settings, Privacy settings and Account are separate pages; child actions stay in Children; Reports opens the selected child's report directly. |
| D-058 | 3, 6 | P1 | DONE | authenticated child lifecycle + route accessibility contracts | The roster and child-detail workflow use compact primary actions, opaque focused dialogs, bounded responsive sections, keyboard-safe controls and 44-pixel targets rather than scattered actions and competing translucent layers. |
| D-059 | 8, 10 | P0 | DONE | clean Docker/Supabase reconstruction + live policy verifier | The orphaned Lima database listener was removed without altering migrations. One Docker-backed target then applied all 38 migrations, started Auth/REST/PostgreSQL healthily, loaded the deterministic audit seed and passed the full live policy suite. |
| D-060 | 6, 9, 10 | P0 | DONE | authenticated no-class Settings journey | Settings now requires a real access-summary object before reading class data. A signed-in teacher with no selected class can open every settings page without entering the global error boundary. |
| D-061 | 6, 7, 9, 10 | P1 | DONE | authenticated direct-report and hard-refresh journey | Reports passes the chosen child identity directly to the canonical whole-child report, whose Skills view, plain exposure sentence, four-state legend, class/child/report URL and exact hard-refresh restoration all pass. |
| D-062 | 5, 6, 7, 9, 10 | P0 | DONE | current teacher route, smoke, accessibility, device and E2E contracts | All release journeys use and exercise Dashboard, Children, Checks, Reports, Resources and Settings rather than the retired information architecture. |
| D-063 | 8, 10 | P0 | DONE | deterministic seed + repeated stateful live gates | Live writers start from the same audit school and verify semantic bounds, so security, lifecycle and data-rights results do not depend on earlier gate residue. |
| D-064 | 2, 3, 6, 10 | P1 | DONE | key-route, emphasis-budget and student-device visual matrices | Story Quest statistics remain compact and the current Adventure Map stop hint avoids edge clipping across supported layouts. |
| D-065 | 8, 9, 10 | P0 | DONE | `check:domain-boundaries` + preview route gates | Preview and release helpers use registered boundary shapes, valid UUID fixtures and the alias-independent direct-access ban. |
| D-066 | 8, 10 | P0 | DONE | `check:recovery-drill` | A real isolated restore verifies protected counts and fingerprints and writes a secret-free recovery artifact. |
| D-067 | 8, 10 | P0 | DONE | `check:learner-data-rights` | The forward JSONB section-count migration permits the real atomic delete and zero-residual proof without weakening audit retention. |
| D-068 | 8, 9, 10 | P0 | DONE | release-target preflight + canonical full run | Frontend and audit Supabase targets must exactly match, preventing orphaned servers or split evidence. |
| D-069 | 8, 10 | P1 | DONE | `check:class-access-security` | The live test verifies required privacy-safe meanings and the documented 3–20 event bound after a real 11-request lockout. |
| D-070 | 3, 8, 10 | P1 | DONE | teacher accessibility + learner-settings + external route readiness | A valid reserved UUID plus explicit empty preview telemetry prevents both malformed and permission-denied sync-health requests without granting anonymous table access. |
| D-071 | 3, 6, 9, 10 | P1 | DONE | contextual-help + authenticated teacher console-clean journeys | View transitions are limited to student sessions; teacher route changes stay immediate and no longer emit DOM-update timeout errors. |
| D-072 | 6, 8, 10 | P0 | DONE | `check:app-copy` + `check:learner-data-rights` | The backend verifier now requires the same canonical “Sign-in tokens” wording as the rendered copy gate before exercising live export/deletion. |
| D-073 | 2, 3, 10 | P1 | DONE | `check:guided-reading-measure` | Each animated reader page carries its exact page number, and the Level A–C browser contract measures only the visible numbered page after every turn. |
| D-074 | 2, 3, 10 | P1 | DONE | `check:assessment-media-evidence` | Assessment question transitions keep answer text fully opaque, so a media-triggered replacement cannot temporarily lower label contrast. |
| D-075 | 2, 3, 10 | P1 | DONE | `check:device-matrix` | Six obsolete Story Quest baselines were individually compared with the current compact story revision and replaced; all route and fullscreen device journeys retain their functional layout, focus, overflow and control-size assertions. |
| D-076 | 4, 7, 10 | P0 | IN-PROGRESS | reporting evidence-policy units + authenticated individual/class report journeys | The current worktree adds independent-attempt, evidence-window, construct-separation, alias, recency, and no-fabricated-score regressions across `reportingEvidencePolicyIntegrity`, `simpleStudentReports`, and whole-child/class report models. Final integrated unit, build, and authenticated browser evidence is pending; no closure is claimed. |
| D-077 | 4, 7, 8, 9, 10 | P0 | IN-PROGRESS | reset/deletion units + `check:learner-data-rights` + `check:db-policies` + `check:sync-chaos` | The current worktree adds a forward data-integrity migration, scoped practice reset, session revocation, shared-row redaction, idempotency, tombstones, direct-delete denial, and two-phase local-cleanup completion. A fresh local migration rebuild and destructive live journey are still required before DONE. |
| D-078 | 3, 5, 6, 7, 9, 10 | P1 | IN-PROGRESS | `teacherBusyWorkflow` + `check:e2e-teacher` + `check:a11y-teacher` + roster device matrix | The current worktree implements urgency-first Dashboard content, a ten-row roster, focused student layers, matching Assessment/Report funnels, separate settings pages, class-aware Resources, and simplified everyday reports. Final authenticated six-section browser and responsive evidence is pending. |
| D-079 | 4, 6, 7, 9, 10 | P0 | IN-PROGRESS | dashboard evidence/loading/model units + `check:teacher-dashboard-data` | The current worktree records per-source error/truncation, preserves last complete rows, prevents false zero/activity inference, rejects stale class loads, and pauses recommendations on incomplete evidence. Final integrated and browser recovery runs are pending. |
| D-080 | 1, 4, 7, 10 | P0 | IN-PROGRESS | HFW coverage/runtime/media gates + curriculum release/skill contracts | Generated interim audits in the current worktree report 100 of 100 HFW targets across 588 eligible questions and restore 146 Nouns questions; construct and no-audio regressions are present. The complete curriculum, build, and full release gates have not yet been rerun against the final tree. |
| D-081 | 1, 2, 4, 10 | P0 | IN-PROGRESS | trace formation unit contracts + reachable Letter Trace browser/device review | The current scorer and unit suite now model ordered strokes, direction, stroke separation, noise tolerance, and device-rate resampling. Final browser pointer/touch review and integrated test evidence are pending. |
| D-082 | 5, 8, 9, 10 | P1 | IN-PROGRESS | source/live `check:db-policies` + signed-out signup browser journey | The final boundary migration now explicitly restores the name-only school lookup while keeping richer anonymous operations denied. Fresh local catalogue/actor proof and the signed-out browser journey are pending. |
| D-083 | 7, 8, 9, 10 | P0 | IN-PROGRESS | reset propagation + legacy report isolation + live tenant-isolation deletion | Current cleanup code and regression fixtures require explicit teacher ownership for same-name legacy fallback. Final fresh-database tenant-isolation and browser deletion evidence is pending. |
| D-084 | 6, 7, 10 | P1 | IN-PROGRESS | formal class-report truth units + authenticated browser/print journey | The current report removes `null%`, fake minimum bars, `NR`, unsupported reading claims, and empty pages while retaining real printable details. Focused truth contracts have passed during implementation; final integrated browser and print evidence is pending. |
| D-085 | 3, 5, 6, 9, 10 | P1 | IN-PROGRESS | auth/error/roster/EL units + app-copy + teacher a11y journey | Semantic auth forms, input-preserving writes, safe teacher error mapping, and in-product reset confirmation are present in the current worktree. Final complete copy, keyboard, focus, and authenticated failure-path evidence is pending. |
| D-086 | 4, 6, 7, 10 | P1 | IN-PROGRESS | Today briefing/current-skill/secure-transition units + authenticated Dashboard journey | The current worktree scopes focus accuracy to the exact skill, applies recency/minimum evidence, counts first secure transitions only, and suppresses recommendations on incomplete sources. Final integrated and authenticated Dashboard verification is pending. |
| D-087 | 3, 6, 10 | P1 | IN-PROGRESS | `check:a11y-teacher` + teacher busy-workflow source contract | Assessments and each Reports mode now have one main landmark without nesting the individual report's own landmark. Final integrated accessibility evidence is pending. |
| D-088 | 4, 6, 7, 9, 10 | P0 | IN-PROGRESS | assessment-hub/busy-workflow units + authenticated assessment failure/retry journey | Assessment choice and launch now require an explicitly complete student-evidence read and expose loading/retry states otherwise. Final integrated browser evidence is pending. |
| D-089 | 4, 7, 10 | P0 | IN-PROGRESS | EL export evidence-consistency units + authenticated export review | Skills-derived EL conclusions now use independent attempts and the canonical threshold instead of a qualitative one-response mastery mapping. Final workbook/browser evidence is pending. |
| D-090 | 5, 6, 9, 10 | P0 | IN-PROGRESS | route hydration regressions + direct-report/EL/demo-class journeys | Dependent class/student route reads are being sequenced against the resolved class context. Final integrated route and browser evidence is pending. |
| D-091 | 4, 7, 9, 10 | P0 | IN-PROGRESS | assessment-history read-state/export units + authenticated failure/retry export journey | Global history completeness is being made load-bearing for official class print and workbook actions. No closure is claimed until partial/error/truncation paths pass. |
| D-092 | 4, 6, 7, 10 | P0 | IN-PROGRESS | class-dashboard/current-window/Today units + authenticated Dashboard journey | Lifetime activity remains available, while current status inputs and focus evidence now use the canonical 90-day subset. Final integrated/browser evidence is pending. |
| D-093 | 4, 6, 9, 10 | P0 | IN-PROGRESS | teacher class-model/loading/retry units + authenticated dashboard recovery | A missing dashboard row is now incomplete/unknown; only an explicit complete zero-result row becomes Not assessed. Final browser recovery evidence is pending. |
| D-094 | 4, 7, 10 | P0 | IN-PROGRESS | EL builder/store/export recency units + authenticated EL report review | Formal EL current conclusions are being aligned to the canonical 90-day window while retaining stale records only as history. Final integrated evidence is pending. |
| D-095 | 1, 4, 7, 10 | P0 | IN-PROGRESS | Advanced Phonics construct-separation units + workbook review | Sound and printed-pattern reading are being prevented from pooling into a fabricated overall Secure result. Final integrated evidence is pending. |
| D-096 | 4, 6, 7, 10 | P1 | IN-PROGRESS | report-period provenance units + authenticated period/print journey | Descriptive totals and current conclusions are being separated so every printed period matches the records actually counted. Final integrated evidence is pending. |
| D-097 | 4, 6, 7, 10 | P0 | IN-PROGRESS | simple-report denominator/reason units + authenticated report review | Simple report prose is being moved onto canonical like-for-like counts and policy reasons, with no invented perfect or multi-day claim. Final integrated evidence is pending. |
| D-098 | 3, 5, 6, 9, 10 | P1 | IN-PROGRESS | sign-in-picture controller/UI units + authenticated failure/retry journey | Sign-in-picture saves are being changed to explicit success/failure so a failed save preserves the modal and draft. Final integrated evidence is pending. |
| D-099 | 5, 6, 8, 10 | P1 | IN-PROGRESS | deletion copy contracts + authenticated data-rights journey | Both deletion entry points are being aligned to the truthful minimal privacy-record retention statement. Final integrated evidence is pending. |
| D-100 | 5, 6, 9, 10 | P0 | IN-PROGRESS | roster read-state units + authenticated failure/truncation/retry journeys | Explicit roster completeness is being threaded through Today and Students so incomplete reads cannot render as an empty class. Final integrated/browser evidence is pending. |
| D-101 | 3, 5, 6, 9, 10 | P1 | IN-PROGRESS | accessibility-dialog save-state units + authenticated failure/retry journey | Inline pending/error feedback and strict-success closure are being added so a covered page banner cannot be the only save feedback. Final integrated evidence is pending. |
| D-102 | 5, 6, 8, 10 | P0 | IN-PROGRESS | deletion-confirmation units + authenticated data-rights journey | Exact-name confirmation is now required for every permanent deletion, including students whose compact roster summary shows zero activity. Final integrated evidence is pending. |
| D-103 | 5, 6, 7, 10 | P1 | IN-PROGRESS | busy-workflow/copy units + authenticated mixed-activity roster review | Answer-count labels now say “No scored answers” and the action says “Run first assessments,” avoiding a false no-practice claim when Sound Seekers activity exists. Final integrated evidence is pending. |
| D-104 | 4, 6, 7, 10 | P1 | IN-PROGRESS | busy-workflow/copy units + authenticated mixed-skill drawer review | The student drawer now distinguishes the current focus from accuracy across all current skills. Final integrated evidence is pending. |
| D-105 | 5, 6, 9, 10 | P0 | IN-PROGRESS | `teacher-route-races` same-section/history/account cases | Exact synchronous route locking and account-generation checks are present. Final settled-tree integrated evidence is pending. |
| D-106 | 4, 5, 6, 9, 10 | P0 | IN-PROGRESS | direct-report refresh/view-change/Back journey on desktop and mobile | Standalone report views now publish into parent state and URL as one transaction. Final settled-tree integrated evidence is pending. |
| D-107 | 3, 5, 6, 10 | P1 | IN-PROGRESS | teacher E2E fold assertion + class-report Back + accessibility | Open reports hide settled choices, begin inside the viewport, and retain an explicit way back. Final settled-tree integrated evidence is pending. |
| D-108 | 3, 4, 6, 7, 10 | P1 | IN-PROGRESS | legacy scope-label units + authenticated token check + persisted workbook round trip | Legacy stored labels remain immutable while every teacher-facing presentation/export path expands BOY/MOY/EOY. Final settled-tree integrated evidence is pending. |
| D-109 | 3, 5, 6, 8, 10 | P1 | IN-PROGRESS | danger-style contract + authenticated computed-style review | Saved-report Delete is visibly distinct from primary Download in normal, hover, focus, and disabled states. Final settled-tree integrated evidence is pending. |
| D-110 | 5, 8, 9, 10 | P0 | IN-PROGRESS | account-access units + held real admin-response account race | Privileged state is identity- and generation-bound, and a delayed positive admin result cannot publish into Teacher B. Final settled-tree integrated evidence is pending. |
| D-111 | 3, 5, 6, 9, 10 | P1 | IN-PROGRESS | Settings route units + authenticated subsection reload/history/class-change journey | All four subsection URLs retain exact context across rendering and navigation. Final settled-tree integrated evidence is pending. |
| D-112 | 5, 6, 9, 10 | P0 | IN-PROGRESS | route hydration units + failed class/roster deep-link recovery journeys | Incomplete ownership reads preserve the exact retryable report route; complete unowned reads still deny it. Final settled-tree integrated evidence is pending. |
| D-113 | 3, 5, 6, 10 | P1 | IN-PROGRESS | authenticated saved-report disabled computed-style journey | A held delete request now produces an explicit, visibly dimmed disabled state without losing the danger hierarchy. Final settled-tree integrated evidence is pending. |
| D-114 | 2, 3, 6, 7, 10 | P1 | IN-PROGRESS | Story Quest state/contrast units + desktop/mobile Axe matrix | Untouched, in-progress, and completed quests now receive explicit semantic classes and AA-compliant sage colours. Final settled-tree evidence is pending. |
| D-115 | 3, 5, 6, 10 | P1 | IN-PROGRESS | primary-route inventory + compact teacher Axe matrix | The collapsed class label is immediately hidden instead of passing through a low-contrast opacity transition. Final settled-tree evidence is pending. |
| D-116 | 2, 6, 9, 10 | P1 | IN-PROGRESS | authenticated Story Quest teacher-preview console-clean journey | Seed evidence now depends on stable normalized content, preventing an unchanged parent progress merge from retriggering itself. Final settled-tree evidence is pending. |
| D-117 | 2, 3, 9, 10 | P2 | IN-PROGRESS | 3D runtime API unit + standard/forced-colour overlay matrix | The affected game uses animation-frame timing and supported PCF shadows with explicit pause/reset behavior. Final settled-tree evidence is pending. |
| D-118 | 4, 6, 9, 10 | P1 | IN-PROGRESS | current learning-policy/class-summary previews + modal Axe inventory | Preview fixtures now mount current production surfaces and use current reachable controls. Final settled-tree evidence is pending. |
| D-119 | 5, 8, 9, 10 | P0 | IN-PROGRESS | navigation-revision unit + held route-runtime Admin race + repeated learner-data-rights journey | Older saved-view and fresh-login restoration continuations now hydrate data without replacing a newer explicit page. Final settled-tree evidence is pending. |
| D-120 | 5, 6, 9, 10 | P0 | IN-PROGRESS | teacher-navigation-scale route units + preview and authenticated Reports history journeys | Report choice, controller state, and the Reports URL now survive reload, bookmark, Back, and Forward as one state. Final settled-tree evidence is pending. |
| D-121 | 3, 5, 6, 10 | P1 | IN-PROGRESS | teacher-navigation-scale focus contract + keyboard browser journey | Completing the final Reports choice now focuses the explicit “Show the report” action. Final settled-tree evidence is pending. |
| D-122 | 3, 5, 6, 10 | P1 | IN-PROGRESS | Today urgency-allocation units + authenticated shortcut journey | Today uses one three-pupil urgency budget and its assessment shortcut opens Assessments. Final settled-tree evidence is pending. |
| D-123 | 3, 5, 6, 10 | P1 | IN-PROGRESS | shared-picker units + 40-pupil Assessments/Reports browser journeys | Both funnels now expose a searchable, eight-per-page pupil picker with bounded keyboard navigation. Final settled-tree evidence is pending. |
| D-124 | 3, 5, 6, 10 | P1 | IN-PROGRESS | pagination-window units + 105-pupil roster browser journey | Children now uses a bounded seven-item page window with end links and non-interactive ellipses. Final settled-tree evidence is pending. |
| D-125 | 3, 5, 6, 10 | P1 | IN-PROGRESS | short-rail CSS contract + exact 667×320 keyboard journey | The teacher rail navigation scrolls independently while every destination and sign-out remains focus-reachable. Final settled-tree evidence is pending. |
| D-126 | 4, 5, 6, 7, 10 | P0 | IN-PROGRESS | EL printable-document unit + authenticated selected-route print-target journey | The EL PDF action now selects a dedicated route-scoped EL document containing real class, student, domain, administration, and measure evidence instead of the generic class report. Final settled-tree evidence is pending. |
| D-127 | 5, 8, 9, 10 | P0 | IN-PROGRESS | four-state account SQL + live table/RPC attack exercise + exact 23-RPC inventory contract | Pending, rejected, and disabled teachers can no longer read or change teacher-owned learning rows through tables or callable privileged teacher RPCs; approved access, administrator access, and learner-token persistence remain available. Final settled-tree evidence is pending. |
| D-128 | 5, 7, 8, 9, 10 | P0 | IN-PROGRESS | exact-store cleanup units + deletion-proof SQL + destructive browser journey | Permanent deletion now checks and reads back the complete browser evidence-store set, fails closed on corrupt/unremovable state, and supplies a fresh subject-bound client cleanup report instead of a bare success claim. Final settled-tree browser evidence is pending. |
| D-129 | 5, 7, 8, 10 | P0 | IN-PROGRESS | shared/singleton intervention deletion SQL + migration contract | Singleton learner interventions are deleted and shared plans retain classmates only after every target name/UUID occurrence is redacted and verified absent. Final settled-tree evidence is pending. |
| D-130 | 4, 6, 7, 10 | P0 | IN-PROGRESS | reporting-workspace, summary, class-dashboard, roster, and simple-report evidence units | One sitting can no longer become Secure through a legacy answer count or an aggregate mastery row; three sittings, eight scored responses, and 85% accuracy are required. Final settled-tree browser evidence is pending. |
| D-131 | 5, 8, 9, 10 | P0 | IN-PROGRESS | held late-stage account-access unit races + authenticated identity-switch journey | Every later administrator-profile, pending-account, and final-publication await is now identity-, authentication-, and generation-bound. Final settled-tree authenticated evidence is pending. |
| D-137 | 2, 3, 9, 10 | P1 | IN-PROGRESS | executable pausable-frame/input units + overlay runtime matrix | Paused frames no longer advance elapsed time, resume starts at zero delta, and held keyboard/touch input is neutralized. Final settled-tree evidence is pending. |
| D-132 | 3, 5, 6, 9, 10 | P0 | IN-PROGRESS | Settings truth units + 8 desktop/mobile authenticated state journeys | Class/privacy/code/deletion controls now require complete current-owner reads and expose truthful loading, incomplete, retry, and empty states. Final settled-tree evidence is pending. |
| D-133 | 3, 5, 6, 9, 10 | P0 | IN-PROGRESS | held code-rotation/reload unit and browser journeys | A validated rotated code remains authoritative and copyable through a failed follow-up reload; retry reloads rather than rotating again. Final settled-tree evidence is pending. |
| D-134 | 3, 5, 6, 8, 9, 10 | P0 | IN-PROGRESS | deletion-refresh truth units + authenticated failed-refresh journey | Verified deletion removes the student from local actions immediately and reports any later roster-refresh failure separately. Final settled-tree evidence is pending. |
| D-135 | 5, 6, 9, 10 | P0 | IN-PROGRESS | class-scoped Settings state units + held A→B response journey | Late class-A responses cannot publish data, feedback, or busy state under class B. Final settled-tree evidence is pending. |
| D-136 | 3, 6, 8, 10 | P1 | IN-PROGRESS | rendered-copy contracts + Settings/privacy browser journeys | Current teacher route names and plain saved/failed/retained language replace stale navigation and backend terminology. Final settled-tree evidence is pending. |
| D-138 | 3, 5, 6, 10 | P1 | IN-PROGRESS | Admin IA source/unit contracts + authenticated desktop/mobile compartment journey | School administration is the plain default area; content, release and technical health tools live under App checks. Final settled-tree evidence is pending. |
| D-139 | 3, 5, 9, 10 | P1 | IN-PROGRESS | admin navigation units + authenticated Back/Forward/reload journey | `/admin/question-flags` now owns the reported-question page and browser history stays synchronized. Final settled-tree evidence is pending. |
| D-140 | 5, 6, 9, 10 | P0 | IN-PROGRESS | production runtime-state checker + 11 SSR state contracts | Fixture-only state claims and generic injection props are removed; only concrete production combinations remain catalogued. Final settled-tree evidence is pending. |
| D-141 | 3, 4, 6, 7, 10 | P0 | IN-PROGRESS | concise-report SSR units + authenticated screen/general-print/EL-print journey | Everyday reporting is bounded and one-page; detailed route-scoped EL reporting remains separate; missing accuracy cannot become 0%. Final settled-tree evidence is pending. |
| D-142 | 5, 8, 9, 10 | P1 | IN-PROGRESS | question-report store units + fresh-database actor/sanitisation SQL + two-browser teacher/admin journey | Assessment question reports now use an idempotent, bounded cloud RPC and private administrator review table; direct writes, foreign/disabled actors, remote image URLs, reviewer spoofing, and false sent acknowledgements are guarded. Final fresh-database and settled-tree browser evidence is pending. |
| D-143 | 3, 5, 9, 10 | P1 | IN-PROGRESS | Admin pathname units + authenticated cold/reload/history/390px/rail-exit journeys | The exact Reported questions pathname now owns the parent Admin view, appears in the compact picker, and is cleared when leaving Admin. Final settled-tree authenticated evidence is pending. |
| D-144 | 5, 6, 9, 10 | P0 | IN-PROGRESS | school-profile units + Settings school loading/error/retry journey | School name has an identity-scoped read state; failure or a missing record cannot become an editable blank, and Save stays absent until a complete matching read succeeds. Final settled-tree authenticated evidence is pending. |
| D-145 | 3, 5, 6, 10 | P1 | IN-PROGRESS | recovery-focus units + authenticated failed-retry/loading/success journey | Retry focus now follows the live loading state to either the replacement recovery action or restored page heading, with bounded observation and visible focus. Final settled-tree accessibility evidence is pending. |
| D-146 | 3, 5, 6, 10 | P1 | IN-PROGRESS | report-focus source/style units + authenticated click/cold-load/reload computed-style journey | Open reports now focus a visible compact context heading with a solid three-pixel outline after click, direct load, and reload. Final settled-tree authenticated evidence is pending. |
| D-147 | 3, 5, 6, 10 | P1 | IN-PROGRESS | report-experience disclosure units + interactive report journey | Each Overview disclosure now owns local open state independently from Show all/Fewer row expansion, so rerenders retain the teacher’s choice. Final settled-tree browser evidence is pending. |
| D-148 | 5, 6, 9, 10 | P1 | IN-PROGRESS | Settings access-read units + mixed summary/history failure browser journeys | Sign-in summary and event history now retain independent class-scoped read states, so one failure cannot clear or relabel the successful sibling result. Final settled-tree authenticated evidence is pending. |
| D-149 | 3, 5, 6, 8, 10 | P1 | IN-PROGRESS | question-review store/note units + authenticated keyboard/review/delete journey | Filters, errors, decision labels, review ownership, and report deletion now describe only the review action actually recorded and never claim that live content changed. Final settled-tree accessibility/browser evidence is pending. |
| D-150 | 2, 3, 6, 7, 10 | P1 | IN-PROGRESS | formal-EL language/report/export units + runner/PDF/Excel browser review | Grade/time, assessment-plan, reading-stage, and next-step language now replaces route/microphase system terminology across teacher EL surfaces and exports. Final settled-tree integrated evidence is pending. |
| D-151 | 3, 5, 6, 10 | P1 | IN-PROGRESS | archived-roster units + 25-student search/pagination browser journey | Archived students are now class-scoped, searchable, bounded to ten rows per page, clamped/reset on context changes, and paired with truthful live counts. Final settled-tree authenticated evidence is pending. |
| D-152 | 5, 8, 9, 10 | P1 | IN-PROGRESS | archive/session SQL + teacher/child archive and reactivation journeys | Both teacher archive paths are being made session-revoking and every child-access boundary is being aligned to active status. No closure is claimed until old and fresh sessions both fail after archive. |
| D-153 | 5, 7, 8, 10 | P1 | IN-PROGRESS | learner-data-rights export/deletion SQL + browser journey | Learner-owned question reports are being added to the private export and complete deletion/residual proof. Final fresh-database evidence is pending. |
| D-154 | 5, 7, 8, 10 | P1 | IN-PROGRESS | shared-record redaction/deletion SQL | Structured and free-text learner references in observations, reviews, and interventions are being removed or rebuilt without deleting classmates' evidence. Final residual scan is pending. |
| D-155 | 4, 6, 7, 10 | P1 | IN-PROGRESS | transfer/report reconciliation units + SQL + authenticated report journey | Former-class evidence is being made visible and reconcilable in owned formal reports without weakening current-class or tenant boundaries. Final settled-tree evidence is pending. |
| D-156 | 5, 8, 9, 10 | P1 | IN-PROGRESS | layered error-ingestion quota SQL + monitoring verifier | Anonymous monitoring limits are being moved from one rotatable fingerprint to layered server-owned windows. Final rotation, privacy, and payload-bound evidence is pending. |
| D-157 | 5, 8, 9, 10 | P1 | IN-PROGRESS | class-access compatibility units + security verifier | Retired anonymous child-login overloads have been removed from the client fallback; final settled-tree, clean-database, and browser evidence is pending. |
| D-158 | 5, 8, 9, 10 | P2 | IN-PROGRESS | question-report quota SQL + store units | Durable per-session, learner, and school quotas are being added while preserving idempotent retries and fair access. Final fresh-database evidence is pending. |
| D-159 | 5, 7, 8, 10 | P2 | IN-PROGRESS | subject-only learner-export SQL | Shared learner records are being projected so the requested child receives their evidence without peer names, identifiers, notes, or snapshots. Final mixed-record export proof is pending. |
| D-160 | 5, 8, 9, 10 | P2 | IN-PROGRESS | four-account-state school-creation SQL | Tenant creation and matching are being restricted to approved teachers and administrators. Final non-approved mutation and tenant-isolation proof is pending. |
| D-161 | 5, 8, 9, 10 | P2 | IN-PROGRESS | administrator decision/audit SQL + controller units | Account decision identity and time are being made server-owned through an administrator-only transition. Final forged-write and immutable-history proof is pending. |
| D-162 | 4, 6, 7, 9, 10 | P1 | IN-PROGRESS | report completeness units + held/error browser journeys | Report conclusions and exports are being suppressed until every required source is explicitly complete. Final state-matrix and authenticated evidence is pending. |
| D-163 | 4, 5, 6, 9, 10 | P1 | IN-PROGRESS | intervention target SQL + planner units/browser journey | Support plans are being required to contain at least one current active student in both UI and database. Final zero/foreign/stale target proof is pending. |
| D-164 | 3, 4, 5, 6, 9, 10 | P1 | IN-PROGRESS | intervention lifecycle units + multi-day Today journey | Due, delivered, note, review, and completion states are being made explicit and reliably resurfaced on Today. Final lifecycle evidence is pending. |
| D-165 | 3, 5, 6, 9, 10 | P2 | IN-PROGRESS | class-keyed planner state + authenticated class-switch journey | Planner targets are being reset and revalidated whenever class context changes. Final cross-class submission proof is pending. |
| D-166 | 3, 5, 6, 9, 10 | P2 | IN-PROGRESS | expiry reconciliation units + authenticated save/reload journey | Explicit saved `null` is being preserved as Never instead of restoring a stale date. Final server/visible parity evidence is pending. |
| D-167 | 3, 6, 10 | P2 | IN-PROGRESS | auth-copy units + signed-out registration journey | The registration credential is being named Email address consistently. Final rendered-copy and validation evidence is pending. |
| D-168 | 3, 5, 6, 9, 10 | P2 | IN-PROGRESS | intervention mutation SQL/units + authenticated keyboard journeys | Edit, reschedule, cancel, and valid draft deletion are being added with history and ownership controls. Final lifecycle and denial evidence is pending. |
| D-169 | 5, 8, 9, 10 | P0 | EXTERNAL-READY | hosted live-database/policy inventories + authenticated hosted journeys | The clean local schema exposes all 35 currently required RPCs, while the configured hosted project exposes 27. The reviewed migration set is ready, but linking/applying it requires an authorized Supabase deployment identity; no hosted pass is claimed. |

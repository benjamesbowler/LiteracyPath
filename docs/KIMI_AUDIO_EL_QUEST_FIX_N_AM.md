# Kimi Audio Request — EL Skills Quest fixes: "n" and "am"

Two recordings in the EL Skills Block map games are wrong and need re-recording
in the **gold voice** (the clean Kimi voice — same as
`public/audio/child-mode/clean-human/words/splash.mp3` and `sing.mp3`, and the
Dino Pals page narration).

**Attach these reference clips with the request** so the voice matches exactly:
- `public/audio/child-mode/clean-human/words/splash.mp3`
- `public/audio/child-mode/clean-human/words/sing.mp3`
- any Dino Pals `page-001.mp3`

**Non-negotiable rules:**

> - Same warm female gold narrator as the reference clips.
> - Plain text only — **no spelling letters out, ever.** A word is said as one
>   blended word, not letter-by-letter.
> - One clear, unhurried take per file. Natural and warm, never robotic.
> - Flat short A (the /æ/ in "cat") — never a long "ah".
> - Overwrite the same filenames; the app picks them up automatically.

---

## 1. Letter name "n" — must say **"en"**

Currently says the wrong thing (sounds like "noun"). It should be the **letter
name**, said exactly like the letter N in the alphabet: **"en"** (rhymes with
"hen"). One short, clear word.

Folder: `public/audio/letter-names/`

- `n.mp3` — say the letter name: **"en"**

---

## 2. Letter sound "n" — must be the **/n/ sound**

Same map games also play the phoneme. It must be the pure consonant sound
**/n/** — a held "nnn" (as at the start of "net"), **not** the letter name and
**not** a word. One clear sound.

Folder: `public/audio/phonemes/`

- `n.mp3` — the /n/ sound: **"nnn"**

> If, on listening, only one of these two "n" files is actually wrong, just
> re-record that one. Both are listed so the right file gets fixed.

---

## 3. Word "am" — must be said as **one word, "am"**

Currently spelled out as "a… m". It must be the whole word **"am"** said
naturally as one blended word (rhymes with "ham"), flat short A. Never the two
letters.

Folder: `public/audio/child-mode/clean-human/words/`

- `am.mp3` — the word: **"am"** (one blended word, rhymes with "ham")

> Note: there are duplicate "am" takes in other folders
> (`public/audio/child-mode/words/am.mp3`,
> `public/audio/child-mode/clean-human/hfw/am.mp3`). For consistency, overwrite
> **all three** with the same good "am" recording.

---

Drop the files into the exact folders above (same names overwrite the bad
recordings), push as usual, done.

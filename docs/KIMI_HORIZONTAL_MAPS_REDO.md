# Kimi Art Request — Redo Dino & Moonwood wide maps (brighter, no numbers)

The meadow wide map is great — keep it. The **dino** and **moonwood** wide maps
need redoing to match it. Two problems to fix:

1. **No numbers printed on the image.** Do NOT paint stop numbers (1, 2, 3…) or
   any labels/markers on the map. The app adds its own markers on top — the art
   must be a clean scene with just the path and landmarks.
2. **Too dark.** Brighten them to match the cheerful, sunny daytime feel of the
   meadow map. Clear daylight, vivid but warm colours, friendly cartoon style.

## Format (same as before)
- **16:9 landscape**, ~1920 × 1080 (or 2752 × 1536).
- Realistic cartoon, bright and warm, child-friendly (ages 4–7), not babyish.
- No rainbows, **no faces on any objects**, no text, no numbers, no UI markers.
- One clear **winding dirt path** travelling **left → right**, passing each
  landmark in order, wide enough for a character to walk along.

## Dino — `dino-map-wide.webp`
Brighten to a **sunny prehistoric valley in clear daylight** (not dim/dusk).
Journey left → right: valley entrance / mud pits on the LEFT → **volcano on the
far RIGHT**. Landmarks along the path: mud pits → green valley → giant ferns →
fossil creek → eggshell rocks → stomping grounds → lava lookout → volcano.
The volcano can glow but must have **no face**. No numbers anywhere.

## Moonwood — `moonwood-map-wide.webp`
This one is a night forest, so keep the moonlit theme BUT lift the brightness a
lot: **bright full-moon glow, luminous glowing mushrooms and fireflies, clearer
readable path** — magical and inviting, not gloomy or murky. Journey left →
right: glow-mushroom grove on the LEFT → **moon tower on the far RIGHT**.
Landmarks: glow-mushroom grove → firefly hollow → whispering trees → moonlit
pond → starfall clearing → crystal cave → owl's lookout → moon tower.
No numbers anywhere.

## Filenames (drop into a folder like before, e.g. "new kimi maps")
- `dino-map-wide.webp`  (replaces the current one)
- `moonwood-map-wide.webp`  (replaces the current one)

Leave `meadow-map-wide.webp` alone — it's already approved.

---

When you send these back, I'll import/compress them, then fine-tune the stop
positions on all three wide maps in one pass.
